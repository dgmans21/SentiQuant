# 작업 일지

새 작업일마다 최상단에 날짜 섹션을 추가한다 (최신순).

---

## 2026-09-20 (마무리) — 최종 수집 + 라벨링 전량 + 임베딩 재구축, 데이터 기준일 고정

### 결정
- 모델 개선은 여기서 종료(v6 프로덕션 유지). 일일 수집 루틴도 이번 갱신을 끝으로 중단, 데이터 기준일 고정
- 시드 변동 재학습(v7 동일 조건, 시드만 변경, 약 24분 예상)은 하지 않기로 함 → README 한계에 "시드 변동 미측정, 1pp 안쪽 차이는 해석하지 않음"으로 기재할 것

### 수집·라벨링·임베딩
- 수집(API, 18:56:48~18:59:04): 백필 119,973→121,672건(+1,699), 큐레이션 9,503→9,721건(+218). 라벨링 입력(`news_all_for_labeling.csv`) 121,891건, 미라벨 2,549건
- 라벨링(GPU 사용 11%에서 시작 확인): 1,000건 배치(18:59:59~19:11:51, 약 11분 52초) + 1,549건 배치(19:15:01~19:33:00, 약 17분 59초). 속도 1.46~1.48건/초. **121,891/121,891건(100%)**, 중복·누락 0
- 최종 분포: 중립 59,306 / 긍정 27,625 / 무관 22,205 / 부정 12,738 / 파싱실패 17. 이번 2,549건 분포는 중립 1,182 / 긍정 530 / 무관 564(22.1%) / 부정 273 (전체 무관 비율 18.2%보다 약간 높음, 주말 기사 비중 때문인지는 미확인)
- 임베딩 재구축(19:33:32 시작, 파일 저장 19:37:10, 약 3분 38초, 인코딩 3분 22초): **97,684 → 99,669건**(긍정·부정·중립만, 768차원). 메타 행수 일치, 링크 중복 0. 기존 파일은 `backup/data/processed/*_prev_97684.*`에 보존
- 참고: 메타에서 `excess_t1` 값이 있는 기사는 24,695/99,669건(`news_multi_horizon.csv`가 08-29 이후 갱신되지 않음). 이 비율이 이전 인덱스와 같았는지는 확인하지 않음 — 최신 기사일수록 검색돼도 "이후 실제 수익률" 정보가 비어 있음
- 가격 매칭은 이번에 하지 않음(RAG 인덱스에 쓰이는 수익률 파일이 08-29 기준). 백업 3종(백필·큐레이션·라벨 코퍼스) 동기화 완료
- 문서 갱신: README(핵심 결과 표의 규모 숫자 교체, 섹터 수 7→10 정정, "라벨 품질 검증과 모델 교체 보류" 섹션 신설), RAG_DEMO.md(인덱스 99,669건·수익률 있는 기사 24,695건·P@5 평가 한계), DATA_PIPELINE_RULES.md(현황 배너·최소 갱신 절차 5번 절). `portfolio/sentiquant.html`은 이 시점에는 손대지 않았고, 이후 아래 "포트폴리오 갱신"에서 처리함
- **데이터 기준일: 2026-09-19 23시경까지 수집분(수집 완료 시각 09-20 18:59)**, 라벨 121,891건, 임베딩 99,669건. 분류기 v6과 검색 P@5 98.5%(80쿼리)는 이전 인덱스(97,684건) 기준으로 측정된 값이며 새 인덱스로 재평가하지 않음

### 포트폴리오 갱신 (`portfolio/sentiquant.html`, 별도 파일이 아니라 기존 파일 갱신으로 결정)
- 배경: 마지막 수정이 09-08이라 README·RAG_DEMO(121,891건, 인덱스 99,669건, 라벨 품질 검증)와 숫자가 어긋나 있었음. README가 이 파일을 "전체 스토리"로 링크하고 있어 새 파일을 만들면 두 버전이 갈라질 위험이 있다고 판단
- **`portfolio/`는 `.gitignore` 대상이라 git 이력이 없음.** 수정 전에 사용자가 물리 백업을 만들었고, 세션 스크래치에도 수정 전 사본 2개(패치 전, CSS 수정 전)를 남김
- 방법: 이미지 3장이 base64로 박힌 910KB HTML(CRLF)이라 직접 편집 대신 파이썬 패치 스크립트로 수정. 교체 대상이 파일에서 정확히 1번 일치할 때만 적용하고 아니면 중단하는 방식
- **신규 07번 섹션 "재검증"**(한계 앞에 삽입, 번호 /08→/09, 내비에 링크 추가): 재라벨링 그룹별 표(네이버 2,524건 중 1,490건 59.0% / 그 외 5,150건 중 2,981건 57.9% / 합계 7,674건 중 4,471건 58.3%), v7·v8·v9 교차평가 덤벨 차트(이전 라벨 84.91/85.11/84.51% ↔ 새 라벨 89.74/90.21/88.91%), 착시 원인 3가지, 사람 검증 1·2번 시트(2번: 새 33 / 이전 11 / 둘 다 아님 6건 막대), v6 유지 결정. 숫자는 README가 아니라 WORKLOG 원본과 대조
- 기존 구간 수정: 히어로 통계(121,891건, v6 학습 51,566건, 수집 기간 고정 표기), 소개문(3만→12만 건, "스스로 낸 모델 개선까지 교차평가로 철회"), 라벨 분포 차트(62,972→121,891건 분포·막대 폭 재계산), RAG 설명(30,511→99,669건), 푸터 기준일 09-02→09-20, 한계 섹션에 항목 4개 추가(수익률 24,695건뿐, v6·P@5는 당시 기준, 사람 검증 한계, 시드 변동 미측정). v5 시점 차트 라벨(30,511건) 등 당시 기록은 의도적으로 유지
- CSS 2건: 히어로 제목이 "주가 / 를 말해주지"로 끊기던 것을 `word-break:keep-all`로 수정, 모바일 내비가 세로로 깨지던 것을 가로 스크롤로 수정(원래 있던 문제, 링크 1개 추가로 악화되던 상태)
- 검증: 헤드리스 Edge로 데스크톱 전체 렌더링 확인. **모바일은 헤드리스 창 최소 폭 때문에 390px을 정확히 재현하지 못함 → 실기기/개발자도구에서 확인 필요**
- 미결: 프로필 "SentiQuant 2026.08 – 진행 중" 표기는 그대로 둠(프로젝트는 마무리 상태). 가로 슬라이드쇼 형태로의 전환은 "스크롤만으로 파악돼야 한다"는 기존 결론과 충돌해 권하지 않음(스크린샷만 캐러셀로 바꾸는 안은 미결정). 포트폴리오를 더 손볼지는 사용자가 고민 중

---

## 2026-09-19 (3) / 09-20 — v8 학습 + 비-naver 확장 착수

### v8: naver 재라벨링 결과를 같은 v7 스냅샷에 적용 → 재학습
- `data/processed/news_sentiment_v8.csv` 생성: v7 스냅샷(81,383건)에 naver 재라벨링 결과(2,524건 중 스냅샷에 걸친 1,685건)를 적용, 1,031건 라벨 변경 + 무관 40건 제외 → 81,342건(train 56,954/val 12,191/test 12,197)
- 학습: 23:27:57~23:52:04(약 24분 7초, 로그 파일 시각으로 확인). epoch별 val F1: 1epoch 84.34% → 2epoch **85.77%(최고)** → 3epoch 85.66% — `load_best_model_at_end`가 2epoch 체크포인트를 자동 선택
- **결과: `models/klue-bert-qwen-sentiment-v8`, test accuracy 86.12%, F1(macro) 84.85%** (v7: 84.73%/83.39%, 동일 스냅샷·설정)
- 표준오차(0.31pp, n=12,197) 대비 차이 +1.39pp = **약 4.4배** — v5→v6 기준(3배)을 넘는 뚜렷한 개선. **(정정: 09-20 교차평가 결과 이 개선은 채점 정답지가 바뀐 착시였음 — 아래 "교차 평가 결과" 섹션 참조)**
- 혼동행렬 회수율(v7→v8): 부정 82.0%→84.0%, 중립 85.2%→87.7%, 긍정 85.0%→83.9%(소폭 하락 — 중립을 긍정으로 과다판정하던 습관이 줄어든 자연스러운 결과로 해석)
- **주의**: v8의 개선은 "모델이 똑똑해진 것"과 "정답 자체가 덜 모순적으로 정리된 것"이 섞인 결과 — Qwen은 파인튜닝된 게 아니라 매번 같은 가중치로 판단만 다시 한 것(입력이 풍부해져서 결과가 달라졌을 뿐), 실제 가중치가 학습된 건 KLUE-BERT뿐

### 비-naver 확장 착수 (v8 기준으로 애매한 케이스 재탐색)
- `scripts/find_ambiguous_cases_nonnaver.py`: v8 예측 vs Qwen 요약문 라벨 불일치, 비-naver 링크(유효 라벨 62,596건) 대상 → 불일치 6,066건(9.7%) → 중립↔긍정/부정 혼동 **5,931건**
- `scripts/scrape_nonnaver.py`: bs4 기반 스크래핑(설치 없음, CPU만). 23:59:19~00:58:51(약 59분 31초, 0.60초/건 — 100건 시험 때와 거의 동일, 병렬화 안 해서 그만큼 걸림) → **5,931건 중 5,150건 성공(87%)**(articleView 구조 위주 3,667건, 문단밀도 fallback 1,356건)
- `scripts/relabel_nonnaver.py`: relabel_full_body.py와 동일 구조로 제목+본문 재라벨링. 02:10~05:18 사이 소규모 배치(40+120+180+120+180+200+160+240+120+140+250+250+150+250+250건)로 **2,650/5,150건(51%) 처리** 후 중단했다가, 09-20 11:49~15:07에 250건씩 10배치를 이어서 **5,150/5,150건 전량 완료**
- 속도는 0.30~0.64건/초로 계속 들쭉날쭉(원인 재조사: 본문 길이와 무관함을 재확인) — 한밤중에 사용자가 동영상(PotPlayer, GPU 하드웨어 디코더 사용)을 보고 있었던 것이 유력한 원인 후보로 새로 발견됨(VRAM 점유는 적어 안전 기준엔 안 걸리지만 GPU 코어를 나눠 쓰며 속도에 영향 가능) — 다음에 재현되면 이 요인부터 확인할 것
- flip rate: naver 59.0%, 비-naver(현재까지) 59.2% — **두 그룹이 거의 동일한 비율로 수렴**, 우연이 아니라 이 코퍼스의 실제 애매 라벨 비율에 가까운 값으로 추정됨. 누적 교정: naver 1,490 + 비-naver 1,568 = **3,058건**

### 비-naver 재라벨링 완료 (2026-09-20)
- 남은 2,500건을 09-20 11:49~15:07에 250건 단위로 이어서 처리 → **5,150/5,150건 전량 완료**. 마지막 배치 250건은 568초(0.44건/초), flip 128/250(51.2%). 배치 속도는 0.28~0.64건/초로 계속 들쭉날쭉했고(가장 느린 배치 904초), 마지막 배치 시작 때 PotPlayer가 GPU 목록에 잡혀 있었음(VRAM은 9%로 안전)
- **비-naver 최종: 5,150건 중 flip 2,981건(57.9%)** — naver(59.0%, 1,490/2,524)와 거의 같은 비율. **두 그룹 합계 7,674건 처리, 4,471건 교정**
- 라벨 이동표(비-naver, 요약문 라벨 → 본문 라벨): 중립 2,988 → 긍정 1,642 / 중립 855 / 부정 410 / 무관 80 / 파싱실패 1, 긍정 1,580 → 긍정 1,049 / 중립 453 / 무관 48 / 부정 29 / 파싱실패 1, 부정 582 → 부정 265 / 중립 219 / 긍정 77 / 무관 21. 요약문 '중립'의 55%가 본문으론 긍정(naver는 49%) — "중립 = 판단 유보용 기본값" 패턴이 비-naver에서도 재현됨
- 학습 반영 시 제외 대상: 무관 149건(21+80+48), 파싱실패 2건
- 백업: `backup/data/raw/`에 `nonnaver_relabeled.csv`, `nonnaver_scraped.csv` 복사. 결과는 아직 학습 코퍼스에 **미반영**

### v9 학습 결과 (2026-09-20)
- 데이터셋 `data/processed/news_sentiment_v9.csv`: v7과 같은 스냅샷(81,382건)에 naver + 비-naver 재라벨링을 함께 적용. 재라벨링 결과 7,674건 중 5,324건이 스냅샷에 걸렸고(naver 1,685 + 비-naver 3,639) 그중 **3,205건(3.9%) 라벨 교체**(naver 1,031 + 비-naver 2,174), 무관/파싱실패 146건 제외 → **81,236건**(train 56,902 / val 12,162 / test 12,172). 스냅샷 겹침 비율은 비-naver 70.7%(3,639/5,150)로 추정치(67%)와 비슷
- 학습: 17:03:18.9 → 17:27:24.6(로그 파일 시각), **약 24분 6초**(학습 루프 1,373초). epoch별 val F1 87.17% → 87.56% → 88.83%(val acc 88.22 → 88.55 → 89.79%), val loss는 0.296 → 0.322 → 0.411로 상승(과적합 조짐이나 acc/F1은 계속 상승) — `load_best_model_at_end`로 3epoch 선택
- **결과: `models/klue-bert-qwen-sentiment-v9`, test accuracy 88.91%, F1(macro) 87.92%** (표준오차 0.285pp, n=12,172)
- 비교: v7(84.73%/83.39%) 대비 +4.18pp = 표준오차의 **9.7배**, v8(86.12%/84.85%) 대비 +2.79pp = **6.6배**, v6(85.05%/83.63%) 대비 +3.86pp = 7.8배
- 혼동행렬 회수율(v7 → v8 → v9): 부정 82.0 → 84.0 → **84.5%**, 중립 85.2 → 87.7 → **90.3%**, 긍정 85.0 → 83.9 → **88.2%**. v8에서 소폭 떨어졌던 긍정 회수율이 v9에서는 크게 회복
- 사전 추정(단순 비례로 약 89%)과 실제(88.91%)가 일치했으나, 이 일치가 "선형 외삽이 맞다"는 증거는 아님(수확체감이 없었을 뿐, 한 번의 결과)
- **해석 주의**: (1) test 라벨도 약 3.9% 교체된 정답지로 채점한 결과라 v6/v7과 같은 잣대의 비교가 아님 — 이전 라벨 기준 / 새 라벨 기준 교차 평가는 아직 안 함. (2) 사람이 확인한 독립 정답이 없음: 새 라벨도 Qwen이 본문을 보고 매긴 것이라 "Qwen 본문 판단과의 일치도"를 재는 것. (3) 프로덕션(RAG 데모)은 아직 v6 그대로

### 교차 평가 결과 — v8/v9의 "개선"은 채점 정답지가 바뀐 착시였음 (2026-09-20 18:00, `scripts/cross_eval_v7_v8_v9.py`)
- 방법: v9 test 12,172행(이 중 라벨이 바뀐 행 814건 = 6.7%, 그대로인 행 11,358건)을 v7/v8/v9로 각각 추론해서 **같은 행·같은 정답지**로 채점. 재현 확인: v9를 새 라벨로 채점한 값이 학습 로그의 88.91%와 일치
- 결과(정확도): v7 — 이전 라벨 84.91% / **새 라벨 89.74%** / 라벨 안 바뀐 행 90.30%, v8 — 85.11% / 90.21% / 90.70%, v9 — 84.51% / 88.91% / 89.66%
- **핵심**: v7(새 라벨로 한 번도 학습 안 함)이 새 라벨로 채점하면 89.74%로 v9의 88.91%보다 높음 → v7 84.73% → v9 88.91%라는 "+4.18pp"는 정답지가 바뀐 효과(v7만 해도 정답지 변경으로 +4.8pp)였고, 같은 잣대로 비교하면 v9는 개선이 없음
- 라벨이 안 바뀐 행(가장 깨끗한 비교) 짝지은 비교: v9가 v7보다 낮음(v7 우세 355건 vs v9 우세 283건, z=-2.9), v9가 v8보다 낮음(387 vs 269, z=-4.6), v8 vs v7은 v8이 약간 높으나 유의하지 않음(325 vs 279, z=1.9). 이전 라벨 기준 전체에서는 v7 84.91 / v8 85.11 / v9 84.51로 서로 0.6pp 이내
- 바뀐 814건에서 모델이 새 라벨에 동의하는 비율: **v7 81.9%**(새 라벨로 학습 안 했는데도), v8 83.3%, v9 78.4% — 즉 본문 재라벨링은 대체로 "v7이 이미 예측한 쪽"으로 라벨을 옮긴 것(후보를 v7/v8 예측과 라벨이 불일치한 행으로 골랐기 때문). v9가 새 라벨을 학습했는데도 오히려 덜 동의함
- 원인 정리: (1) 재라벨링 후보를 모델 불일치로 골라 교정이 val/test에 몰림(train 2.5% vs val 6.8%·test 6.7%) → test 정답지가 train보다 2.7배 더 정리됨, (2) 정답지를 모델 예측 방향으로 고치면 모델(v7 포함) 점수가 오름, (3) 새 라벨은 본문을 본 판단인데 분류기는 제목+요약만 보므로 일부만 복제 가능 → 소수(3.9%)만 본문 기준 라벨이고 나머지는 요약문 기준이라 라벨링 기준이 섞임(v9가 안 바뀐 행에서 손해)
- **정정되는 기존 기록**: v8 "+1.39pp, 4.4배 — 라벨 품질 개선 효과 실증", v9 "+4.18pp, 9.7배" 모두 채점 정답지 변경의 착시. 같은 잣대에서 v8의 개선은 +0.2~0.5pp(유의하지 않음), v9는 개선 없음(오히려 소폭 낮음). 남는 사실: 요약문 라벨과 본문 라벨은 애매 케이스의 약 59%에서 다르다(라벨링 과정 자체의 측정). 본문 라벨이 더 "정답에 가까운지"는 사람이 검증한 정답이 없어 미확인(정성 확인 5건뿐)
- 한계: 학습 run 간 변동(시드)을 측정한 적 없어 위 z값은 test 행 샘플링 오차만 반영(0.5pp 안팎 차이는 시드 노이즈일 수 있음). 프로덕션 교체·포트폴리오 수치 갱신은 하지 말 것(v6 유지)

### 사람 검증 (블라인드 100건 계획) — 1번 시트는 결론을 못 냄, 2번 시트(강제선택)로 보완 (2026-09-20)
- 방법: 라벨이 바뀐 기사 4,453건 중 무작위 100건으로 블라인드 시트 2개 생성(`data/raw/human_check_sheet.md` 1~50번, `human_check_sheet_2.md` 51~100번, 정답은 `human_check_key.csv`, 생성 `scripts/make_human_check_sheet.py`). 이전/새 라벨은 숨기고 기사만 보여줌. 사용자가 1번 시트 50건에 자유 표현으로 답한 뒤, 2번 시트는 강제선택 규칙으로 별도 진행(아래)
- 답 형식: `판단:` 뒤가 아니라 그 아래 `---` 줄에 적혀 있었고 "약한긍정", "사실전달" 같은 자유 표현 → 규칙으로 매핑(사실전달→중립, 약한 X→X를 기본, 두 라벨을 함께 적은 5건은 양쪽 모두 일치로 인정)
- 결과(단일 라벨 답 46건): 사람 판단이 새 라벨 쪽 28 / 이전 라벨 쪽 11 / 둘 다 아님 7(복합 답 포함 50건이면 30/12/7, 둘 다 1). 방향별: 중립→긍정 26건 중 22건이 새 라벨(85%), 반면 긍정→중립·부정→중립 10건은 새 라벨 1 vs 이전 라벨 6
- **민감도(핵심)**: "약한 긍정/부정"(18건)을 중립으로 읽으면 새 라벨 비율이 72% → 41%(17 vs 24)로 뒤집힘. 확신해서 적은 답(28건)만 보면 새 14 vs 이전 10으로 유의하지 않음. 사용자 답이 긍정 쪽으로 치우침(32/46=70%)도 감안 필요. → **(1번 시트만으로는) 새 라벨이 더 정답에 가깝다고 결론 낼 수 없음 — 2번 시트로 아래처럼 보완됨**
- 의미: 쟁점은 "약한 긍정이 긍정인가 중립인가"라는 **경계 정의**가 미해결이라는 것 — Qwen(요약문 기준/본문 기준)도 사람도 같은 선을 못 긋는다. 사용자는 판단이 매우 어렵다고 함(그 자체가 이 과제의 라벨 모호성을 보여주는 관측). v8/v9에서 분류기 개선이 안 나온 것과도 앞뒤가 맞음
- **2번 시트 결과(강제선택, 51~100번, 09-20 18:46 저장)**: 사용자가 `약한`·`?` 없이 긍/중/부/무 한 글자로 답함. 규칙은 "기사에 방향을 직접 말하는 표현(증가·호실적·수주·성장·상승 / 감소·부진·적자·하락·리스크·소송)이 있으면 그 방향, 없으면 중립"(Qwen 프롬프트 기준과 같은 뜻). 답은 `판단:` 아래 줄에 적힘. **사람==새 라벨 33건(66%), 사람==이전 라벨 11건(22%), 둘 다 아님 6건(12%)** → 둘 중 하나와 일치한 44건 중 새 라벨 75%(95% CI 61~85%, 부호검정 p≈0.001)
- 방향별(2번 시트): 중립→긍정 27건 중 새 21 / 이전 5(78%), 긍정→중립 8건 중 새 5 / 이전 0, 부정→긍정 2건 중 새 2 / 이전 0, 중립→부정 10건은 새 4 / 이전 5(지지 없음). 사람 분포 긍 23 / 중 17 / 부 5 / 무 5(새 라벨 29/10/10/1, 이전 라벨은 중립 38·긍정 8·부정 4)
- **해석**: 엄격한 기준으로 보면, 요약문 기반 라벨과 본문 기반 라벨이 갈리는 기사에서 사람은 약 3:1로 본문 기반(새) 라벨 쪽에 동의함 → 그 flip 대상에서는 새 라벨이 더 정답에 가까울 가능성이 높음. 지지는 중립→긍정·긍정→중립 flip에 집중, 중립→부정 flip은 지지 없음. 1번 시트에서 결론이 뒤집혔던 건 "약한 긍정"을 어떻게 읽느냐 때문이었고, 규칙을 고정하자 해소됨. 한계: 평가자 1명·n=50, 사용자 답이 긍정 쪽으로 치우침(46%), 기준이 Qwen 프롬프트에서 따온 것이라 완전히 독립된 정답은 아님, flip된 기사만 본 것이라 전체 라벨 정확도는 아님
- **분류기와의 관계**: 새 라벨이 더 정답에 가까워 보이는데도 v8/v9 분류기가 좋아지지 않은 것은 모순이 아님 — 분류기는 제목+요약만 보고 본문 정보는 못 보며, 교정은 전체의 3.9%뿐이고 나머지는 요약문 기준 라벨이라 학습 신호가 섞임. "라벨이 더 나아졌다"와 "그 라벨로 분류기가 좋아진다"는 별개

### (이전 기록) 다음 재학습(v9) 시점의 낙관적 추정 (검증 전, 단순 비례 계산)
- v7 스냅샷(81,383건) 기준으로 볼 때, naver 교정 중 스냅샷에 걸린 비율(67%)을 비-naver에도 그대로 적용하면 총 교정 비율이 v8 때(1.27%)의 약 3배(~3.8%) 예상 → 단순 비례 가정 시 +4.2pp, **test accuracy 약 89% 안팎**으로 추정
- 다만 이건 순진한 선형 외삽이고, 수확체감(나중에 고치는 케이스일수록 더 애매함) 가능성이 높음 — 사람 간 감성 라벨 일치율이 보통 80~90%대인 걸 감안하면 89%는 이미 그 천장 근처. 실제 값은 v9을 돌려봐야 확인 가능
- 방법론: v8과 마찬가지로 **v7과 같은 스냅샷에 라벨만 교체**해서 순수 라벨 효과를 봐야 함(데이터 볼륨 증가와 섞으면 안 됨). 비-naver 재라벨링은 09-20에 전량 완료됐으므로 진행 가능(미착수)

### 기타
- 오늘 낮에 한 일반 뉴스 수집(백필 119,126→119,973, 큐레이션 9,353→9,503)의 backup 동기화를 지금 완료함(main 라벨 코퍼스는 오늘 안 건드려서 `news_qwen_labeled.csv`는 그대로)
- 새 스크립트 6개 미커밋: `find_ambiguous_cases_nonnaver.py`, `scrape_nonnaver.py`, `relabel_nonnaver.py`(위 3개는 오늘 신규), 어제부터 미커밋인 4개(`find_ambiguous_cases.py`, `relabel_full_body.py`, `test_generic_extraction.py`, `validate_full_body_labeling.py`)

---

## 2026-09-19 (2) — 라벨 품질 개선 (요약문 → 본문 재라벨링)

### 한 줄 요약
- v7 오류 분석에서 "요약문만으로는 중립↔긍정/부정 경계를 놓친다"는 가설을 세우고, 본문 스크래핑 + Qwen 재라벨링으로 검증 → 본문 라벨이 요약문 라벨과 애매 케이스의 59%에서 다르다는 것까지 확인(**분류기 성능이 실제로 좋아진다는 것은 09-20 교차평가에서 확인되지 않음**). naver 링크의 애매한 케이스 2,524건을 전량 재라벨링했고 flip 59.0%. 결과는 아직 학습 코퍼스에 **반영하지 않았고**, v8 재학습은 나중에 하기로 함.

### 방법 (신규 스크립트 4개, 모두 미커밋)
- `scripts/find_ambiguous_cases.py`: v7 예측과 Qwen(요약문 기반) 라벨이 불일치하는 중립↔긍정/부정 혼동 케이스를 찾음. naver.com 링크(본문 스크래핑 가능 범위)만 대상
- `scripts/validate_full_body_labeling.py`: naver 뉴스 페이지(`#dic_area`) 본문 스크래퍼. CPU만 사용, 입출력 경로를 인자로 받음, 200건마다 저장
- `scripts/relabel_full_body.py`: 제목 + 본문(최대 1,500자)으로 Qwen 재라벨링. 건수를 인자로 받고 40건마다 체크포인트, 링크 기준 재시작, BATCH_SIZE 8. 모델 로딩 시간과 생성 속도를 출력
- `scripts/test_generic_extraction.py`: 비-naver 링크에서 사이트별 규칙 없이 본문을 뽑을 수 있는지 시험(bs4만 사용, 설치 없음)

### 대상 범위
- 유효 라벨(긍정/부정/중립) 97,684건 중 naver 링크 30,485건(31%) → v7-라벨 불일치 2,611건(8.6%) → 중립↔긍정/부정 혼동 **2,528건**이 대상. 본문이 없는 4건을 뺀 2,524건 처리
- v7 학습 스냅샷(9/14 밤) 이후의 신규 기사도 대상에 섞여 있음

### 결과
- 총 flip **1,490/2,524 = 59.0%** (첫 150건 검증 67.3%, 이후 구간별 45~62%로 변동)
- 라벨 이동(요약문 라벨 → 본문 라벨): 중립 1,550 → 긍정 765 / 중립 474 / 부정 269 / 무관 42, 긍정 620 → 긍정 400 / 중립 199 / 부정 18 / 무관 3, 부정 354 → 부정 160 / 중립 137 / 긍정 38 / 무관 19. 본문 기준 분포: 긍정 1,203 / 중립 810 / 부정 447 / 무관 64
- 요약문 기준 '중립'의 49%가 본문으로는 긍정이었음 — 중립이 "판단 유보용 기본값"처럼 쓰였다는 뜻
- 정성 검토(flip 5건 직접 확인)는 전부 합리적인 정정이었음 (현대차 임협 타결 중립→긍정, KB금융이 코스피 동향 기사에 스쳐 지나간 것 부정→중립, 카카오 기사에 SK텔레콤 지분이 언급됐을 뿐인 오탐 중립→무관 등)
- **해석 주의**: flip 비율은 라벨 오류율이 아님(v7과 불일치한 표본만 뽑았으므로 편향됨). 구간별 flip 변동(45.0 / 51.2 / 62.1%)의 원인은 미확인 — "v7이 못 본 신규 기사라 낮다"는 가설은 마지막 구간이 62.1%로 올라 폐기

### 실행 시간 · GPU 관찰
- 속도: 정상 실행은 0.33~0.75건/초(평소 라벨링 1.3~1.5건/초의 약 1/3~1/2). 이상 실행이 있었음 — 10건 테스트 2회(133초·151초 = 0.07건/초), 200건 배치 78분. **원인 미확인.** "본문이 길어서 memory-bound라 원래 느리다", "절전모드로 멈췄다"는 두 설명은 모두 근거 부족으로 폐기
- 배치(8건)당 시간이 약 10.5초 vs 27~33초로 갈리며, 느린 배치는 전력이 ~57W로 떨어짐(사용률 100%, 클럭 동일). 응답 길이·본문 길이와 무관(스피어만 0.01~0.2), 실행별로 몰려 나타남(0~7/30). 동시에 돌린 추출 시험과도 무관(느린 배치가 그 전후에도 나타남). 원인 미확인
- 실행 전 다른 프로그램의 VRAM 점유와 속도: 이상 실행(2.2~3.0GB → 0.07건/초)만 뚜렷하고, 정상 범위(≤1.1GB) 안에서는 무관(712MiB → 0.54, 705MiB → 0.75). 순위상관 -0.71이지만 n=6이라 유의하지 않음
- VRAM: 생성 구간 내내 7.8~7.9GB, 최대 7,935MiB(96.9%), 여유 약 250~300MB. 평소 라벨링 실행 중은 약 6.0GB(74%, 09-14 1회 측정) — 이전에 적었던 "평소 15~20%"는 실행 전 유휴 값이라 잘못된 비교였음. 원인: 입력 토큰이 3배(프롬프트 평균 426 → 1,236, 8건 배치 패딩 448 → 1,493)라 KV캐시가 0.58 → 1.73GiB(토큰당 144KiB)로 커진 것이 약 1.2GB를 설명하고, 나머지는 미측정. 총량이 항상 96~97%로 같아서 할당자가 남는 공간까지 채우는 것으로 보임
- 온도: 대부분 63~68도, 최대 72도, 전력 평균 90~126W — 안전 범위
- 이 작업이 도는 동안은 다른 GPU 작업 금지(여유가 얇음). BATCH_SIZE 8→4는 검토 후 사용자 결정으로 8 유지

### 비-naver 확장 검토 (미착수)
- 비-naver 유효 라벨 62,596건(669개 도메인). 도메인 수로는 분산(상위 10개 14%, 상위 100개 60%)이지만 URL 구조로는 **63.9%(39,991건, 429개 도메인)가 `/news/articleView.html?idxno=`**
- 100건 시험(bs4만, 본문 추출 + 네이버 리드문 일치로 검증): **85/100(85%)**, 95% 신뢰구간 약 77~91%. 해당 구조 64/66(97%, 선택자 `#article-view-content-div` 하나로 63/64), 그 외 21/34(62%, 문단밀도 fallback은 8/17)
- 규모(미측정 추정): naver와 같은 8.3%를 적용하면 애매 케이스 약 5,200건, 성공률을 곱하면 약 4,400건, GPU 재라벨링 약 2시간
- trafilatura 설치는 보류(이 환경은 예전에 pip install이 torch를 덮어쓴 적이 있음, bs4 선택자로 충분). 추출은 GPU 없이 CPU/네트워크만 쓰므로 GPU 작업과 동시에 가능

### 기타 기록
- v7 학습 시간 실측(로그 파일 시각): 09-14 23:04:05 → 23:28:17 = **약 24분**(학습 루프 1,382초). 당시 대화에서 말한 "22:38 시작·41분·총 1시간"은 확인하지 않은 시각에 근거한 오류였음 — 시각은 추측하지 말고 `stat -c '%w %y'`나 `date`로 확인할 것
- 백업: 결과 `full_body_relabeled_full.csv`와 스크래핑 본문 `full_body_scraped_full.csv`를 `backup/data/raw/`에 복사(기존 3종 정책 외 예외)
- 임베딩(97,684건)은 기존 라벨 기준이며 재라벨링 결과는 미반영

### 다음 (미결정)
1. flip 라벨 교체(무관은 학습 제외) → v8 재학습(같은 split · 3 epoch · 같은 설정, 예상 약 25분) → 이전 라벨 기준과 새 라벨 기준 둘 다로 평가해 v6/v7과 비교
2. v8 효과를 본 뒤 비-naver 확장 여부 결정
3. 일일 수집·매칭 루틴 유지, 임베딩은 라벨이 크게 늘거나 라벨을 교체한 뒤 갱신
---

## 2026-09-19

### 뉴스 재수집 + 가격 매칭 + 라벨링 전량 완료 + 임베딩 재구축
- 백필 115,115→119,125건(+4,010), 큐레이션 8,905→9,352건(+447) — 토요일 새벽 수집이라 평소보다 적었음(정확한 원인은 요일 때문인지 수집 간격 때문인지 불확실, 기술적 에러는 없었음)
- 가격매칭 110,621/119,342건(신규 8,721건 보류, 금요일 종가까지만 반영)
- 라벨링 1000건×3회+1100건, 총 4배치로 **119,342/119,342건(100%) 완료**
- 최종 분포: 중립 48.7%(58,124) / 긍정 22.7%(27,095) / 무관 18.1%(21,641) / 부정 10.4%(12,465) / 파싱실패 17건
- **임베딩 재구축**: 81,382건 → 97,684건. 실측 소요시간 약 3분(전회 81,382건 기준) — 재학습과 달리 검증 절차 불필요해서 라벨링 끝날 때마다 자주 갱신하기로 함
- 백업 3종 동기화 완료

---

## 2026-09-17 (2) / 09-18

### 뉴스 재수집(2회) + 가격 매칭 + 라벨링 전량 완료
- 백필 110,395→115,114건, 큐레이션 8,376→8,904건 (저녁+심야 2회 수집)
- 가격매칭 110,621/115,331건(신규 4,710건 보류)
- 라벨링 1000건×4회+720건, 총 5배치로 **115,331/115,331건(100%) 완료**
- 최종 분포: 중립 48.7%(56,131) / 긍정 22.6%(26,070) / 무관 18.2%(20,985) / 부정 10.5%(12,129) / 파싱실패 16건
- v6 기준 +83.1%, v7 기준 +41.7% — 여전히 재학습은 보류(라벨 품질 개선 우선 방침 유지)
- 가격매칭은 GPU 불필요(CPU/API 호출만) 확인 — 라벨링 배치와 동시 진행 가능
- 백업 3종 동기화 완료

---

## 2026-09-16 (2) / 09-17

### 뉴스 재수집 + 가격 매칭 + 라벨링 전량 완료
- 백필 105,286→110,394건(+5,108), 큐레이션 7,894→8,375건(+481)
- 저녁 재확인으로 매칭 105,519/110,611건(신규 5,092건 보류)
- 라벨링 1000+1000+1100+1005+997건, 총 5배치로 **110,611/110,611건(100%) 완료**
- 최종 분포: 중립 48.2%(53,362) / 긍정 22.7%(25,064) / 무관 18.5%(20,480) / 부정 10.6%(11,689) / 파싱실패 16건
- v6 기준 +75.7%, v7 기준(81,383건) +35.9% — v7이 이미 증가폭만으로 개선 안 됐던 전례가 있어 재학습은 계속 보류, 라벨 품질 개선 과제가 우선
- 백업 3종 동기화 완료

---

## 2026-09-15 (2) / 09-16

### 가격 매칭 100% + 뉴스 재수집 + 라벨링 전량 완료
- 9/15 저녁 매칭 100,498/100,498건(100%) 확인 → 뉴스 재수집(백필 100,283→105,285(+5,002), 큐레이션 7,448→7,893(+445)) → 새벽이라 매칭은 95,071/105,502건(신규 10,431건 보류)
- 라벨링 1000+1200+1000+1000+990건, 총 5배치로 **105,502/105,502건(100%) 완료**
- 최종 분포: 중립 48.1%(50,709) / 긍정 22.6%(23,837) / 무관 18.7%(19,700) / 부정 10.7%(11,241) / 파싱실패 15건
- v6 기준 +67.5%, v7 기준(81,383건) +29.6% — v7이 개선 없었던 전례가 있어 이 증가폭만으로 재학습 시기를 단순 판단하지 않기로 함(라벨 품질 개선이 우선 과제라는 결론 유지)
- 백업 3종 동기화 완료

---

## 2026-09-14 (3) / 09-15

### v7 재학습 — 개선 없음, "성능 정체" 재논쟁
- split 재생성(stratified, 81,383건: train 56,968/val·test 각 12,207~12,208) → `models/klue-bert-qwen-sentiment-v7` 학습
- **test accuracy 84.73%, F1(macro) 83.39%** — v6(85.05%/83.63%) 대비 -0.32%p, **표준오차(≈0.33%p) 이내라 통계적으로 구분 안 됨**
- v5→v6(+69% 데이터)는 확실히 개선됐는데 v6→v7(+59.3%)은 개선 없음 — "정체가 아니라 증가폭 부족"이라던 이전 결론이 다시 흔들림. klue-bert-base 아키텍처가 83~85% 근방에서 실제 천장에 도달했을 가능성 재부상
- 혼동행렬 확인: 중립실제→긍정예측 오분류가 800건으로 최대 오류 블록 — 중립/긍정 경계가 애매한 게 원인일 가능성 (Qwen 라벨 자체의 경계 불안정성일 수도 있음)
- **결정: v6을 프로덕션 모델로 유지**, v7은 개선 근거 부족으로 교체 보류
- 다음 시도 후보(효과 클 것 같은 순): ①애매 케이스(중립↔긍정) 본문 스크래핑 후 재라벨링으로 라벨 품질 개선 ②klue-roberta-large 등 다른 백본 시도 ③하이퍼파라미터 튜닝(lr/epoch/max_length) — 아직 미착수

### 당일 마무리 뉴스 수집
- 백필 100,096→100,282(+186), 큐레이션 7,396→7,447(+51) — 자정 무렵이라 소량

### 임베딩 재구축
- 분류기(v6/v7)와 임베딩(ko-sroberta-multitask)은 완전히 별개 모델이라는 점 재확인 — 분류기 버전과 무관하게 임베딩은 최신 라벨 데이터로 갱신 가능
- 임베딩 인덱스가 v6 학습 시점(51,566건)에서 멈춰있던 걸 발견 → 최신 라벨 코퍼스(81,382건, 무관/파싱실패 제외)로 재구축 완료. RAG 데모 유사사례 검색 풀에 그동안 누락됐던 약 3만 건이 새로 포함됨
- 백업 3종 파일 최종 동기화 완료

---

## 2026-09-14 (2)

### 당일 추가분까지 라벨링 완료 — v6 대비 +59.3%
- 당일 신규 수집분(5,250건) 포함해서 1000건×5회+1250건, 총 6배치로 **100,312/100,312건(100%) 완료**
- 최종 분포: 중립 48.1%(48,242) / 긍정 22.4%(22,448) / 무관 18.9%(18,916) / 부정 10.7%(10,692) / 파싱실패 14건
- v6(62,972건) 대비 **+59.3%** — v5→v6 때 확실한 개선을 냈던 증가폭(+69%)에 근접

### 운영 이슈: 배치가 인터럽트 후에도 백그라운드에서 계속 실행됨
- 대화상 "중단"이 실제 OS 프로세스를 안 죽이는 경우 확인 — 인터럽트 직후 재확인했더니 이전 배치(PID 14772)가 VRAM 76%·GPU 92%로 여전히 돌고 있었음
- 그 상태에서 새 배치를 또 띄우면 동시 실행이 돼버려 위험했음 — 재확인 후 기존 것이 끝날 때까지 새 배치를 미루는 방식으로 대응. **앞으로 인터럽트 이후에도 GPU 상태를 다시 확인하기 전엔 새 배치를 시작하지 않기**

### 논조-요약문 신뢰도에 대한 미검증 한계 확인
- "NAVER API의 description이 본문 전체 뉘앙스를 얼마나 잘 대표하는가"라는 질문에 답하면서, 이게 실제로 검증된 적 없는 가정이라는 걸 인지함 — description은 AI 요약이 아니라 리드 문단 스니펫(~200자)이고, 스트레이트 뉴스는 잘 대표할 가능성이 높지만 반전형 기사·클릭베이트성 헤드라인은 놓칠 수 있음. 본문 스크래핑 후 재라벨링해서 label flip rate를 재보는 검증은 아직 안 함(향후 과제)

---

## 2026-09-14

### 가격 매칭 100% 회수 + 라벨링 전량 완료 (+50% 재학습 기준선 돌파)
- 주말(9/12~13) 동안 매칭 보류됐던 물량이 월요일 저녁 장 마감 반영 후 한꺼번에 회수됨: **95,062/95,062건(100%) 매칭**
- 라벨링도 1000건×5회 배치로 **95,062/95,062건(100%) 완료**. 최종 분포: 중립 47.9%(45,539) / 긍정 22.5%(21,364) / 무관 19.2%(18,266) / 부정 10.4%(9,879) / 파싱실패 14건
- v6(62,972건) 대비 **+51.0%** 증가 — 재학습 검토 기준(+50%)을 처음으로 돌파함. 다음 세션에서 재학습(v7) 진행 여부 결정 예정
- 이어서 당일분 뉴스 추가 수집: 백필 94,847→100,095(+5,248), 큐레이션 6,945→7,395(+450)

### 재학습 판단 관련 논의
- "가격매칭 안 되면 모델 만드는 게 의미 있나"라는 질문에 답함 — 분류기(klue-bert-qwen-sentiment)는 텍스트+Qwen라벨만으로 학습되고 가격매칭 데이터는 전혀 쓰이지 않음(매칭은 RAG 데모의 "실제 수익률" 부가정보와 상관관계 분석 전용). 매칭 지연 여부는 재학습 타이밍과 무관하다는 점 확인
- 주말엔 장이 안 열려 매칭 불가 확인(토·일 반복 확인), 평일 저녁(장마감+반영지연 이후)에 재시도하면 회수됨

---

## 2026-09-12

### 라벨링 전량 완료 (수집·매칭은 9/11에 이미 진행)
- 9/11에 수집한 90,162건 대상으로 500+1000+385+500+1000+989건, 총 6배치로 **90,162/90,162건(100%) 완료**
- 최종 분포: 중립 47.9%(43,145) / 긍정 23.0%(20,753) / 무관 18.5%(16,637) / 부정 10.7%(9,613) / 파싱실패 14건
- 매칭은 9/11 기준 85,734/90,162건 그대로(오늘 추가 수집 안 함)

### GPU 관련 소통 계속 준수
- 매 배치 전 nvidia-smi 체크, 명시적 지시("500건", "1000건" 등) 있을 때만 배치 시작 — [[feedback_ask_before_gpu_runs]] 규칙대로 진행

### 재학습(v7) 타이밍 판단 (계속)
- v6(62,972건) 대비 지금(90,162건) 증가폭은 +43.2% — +50%(약 94,458건)까지 약 4,300건 남음. 거의 근접

### 면접 예상 질문 추가 대응
- 실제 면접에서 받은 질문("KLUE-BERT가 이미 토큰화하는데 왜 Qwen으로 라벨링하나", "배치 단위 처리가 뭘 말하는지", "종목당 1000건 API 한도가 맞는지")에 대해 구두로 답변 정리 — 토큰화(전처리)와 라벨링(의미 판단)의 차이, Qwen=라벨 생성용 티처 모델·KLUE-BERT=실서비스용 경량 분류기 역할 분담, NAVER API 1000건 한도가 종목(검색어) 단위라는 점(하루 25,000건 호출 한도와는 별개) 확인함

---

## 2026-09-11

### 뉴스 재수집 + 가격 매칭 + 라벨링 전량 완료
- 백필(20종목) 79,602 → 85,557건(+5,955), 큐레이션(삼성·SK하이닉스) 5,622 → 6,091건(+469)
- 가격/지수 갱신 후 재매칭: 69,546 → **74,188건**(+4,642) — 어제 보류됐던 9/8·9/9 발행분이 오늘 회수됨(yfinance 반영 지연이 원인이었음, [[project-model-training]] 참고). 남은 11,585건은 여전히 다음 거래일 종가 대기
- 라벨링 대상 85,773건(신규 +5,957건) → 1000건×5회+957건, 총 6배치로 **85,773/85,773건(100%) 완료**
- 최종 분포: 중립 47.6%(40,811) / 긍정 23.3%(20,005) / 무관 18.6%(15,926) / 부정 10.5%(9,017) / 파싱실패 14건

### 면접 예상 질문 답변 준비 (계속)
- 사용자가 실제 면접에서 받을 법한 꼬리질문들(가설기각 판단 근거, Qwen 자동라벨링 설계 이유, enable_thinking=False, split 버그 전후 비교, 효과크기 실전 활용법, 규칙기반 필터 포기 이유 등)에 대한 답변을 세션 중 구두로 정리해줌 — 문서화하지 않고 대화로만 진행, 필요시 다시 요청 예정

### 재학습(v7) 타이밍 판단 (계속)
- v6(62,972건) 대비 지금(85,773건) 증가폭은 +36.2% — +50%(약 94,000건) 도달까지 약 8,700건 남음

---

## 2026-09-10

### 뉴스 재수집 + 가격 매칭 + 라벨링 전량 완료
- 백필(20종목) 73,954 → 79,601건(+5,647), 큐레이션(삼성·SK하이닉스) 5,136 → 5,621건(+485)
- 가격/지수 이력 갱신 후 재매칭 시도 — 9/9 종가가 전 종목 NaN으로 아직 안 채워져 있어(yfinance 반영 지연) 매칭 수치는 69,546건 그대로, 신규분(9/8·9/9 발행분 10,270건)은 보류
- 라벨링 대상 79,816건(신규 +5,648건) → 650+1000×4+992건, 총 6배치로 **79,816/79,816건(100%) 완료**
- 최종 분포: 중립 47.0%(37,550) / 긍정 23.6%(18,838) / 무관 18.6%(14,861) / 부정 10.7%(8,553) / 파싱실패 14건 — 비율 안정적으로 유지
- 배치당 소요시간 실측 시작: 1000건에 약 6분(1.4~1.5건/초) — 예상보다 빠르다는 반응
- 매 배치 전 GPU 상태 확인, 도중 Ollama 등 다른 프로세스 감지 시 대기하는 방식으로 안전 규칙 준수

### GPU 배치 시작 관련 소통 정리
- "배치완료되면 말해줘"를 "다음 배치도 알아서 이어가라"로 확대해석해 사용자 허락 없이 새 배치를 시작한 일 발생 → 사용자가 "지금하지마", "허락안받고돌리지마"로 정정
- 사용자 의도 확인: 완료 보고는 "GPU를 비워서 잠깐 다른 용도로 쓰려던" 신호였음 — 앞으로는 완료 시 보고만 하고 명시적 지시 있을 때만 다음 배치 시작하기로 함 (`feedback_ask_before_gpu_runs.md`에 기록)

### 면접용 브리핑 문서 제작
- 포트폴리오(`portfolio/sentiquant.html`) 내용을 바탕으로 면접에서 바로 설명/보고할 수 있는 요약 문서를 Artifact로 제작 — 가설기각→전환→버그발견→상관관계 재검증→RAG도구화→한계, 타임라인 구조 + 예상 질문/답변 섹션 포함
- 사용자가 이 프로젝트를 면접용 포트폴리오로 쓸 계획이라는 것 확인 (`user_career_context.md`에 기록)

### 재학습(v7) 타이밍 판단 (계속)
- v6(62,972건→51,566건 학습) 대비 지금(79,816건) 증가폭은 +26.7% — 아직 유의미한 개선 기대할 수준(+50~69%)엔 못 미침. 10만 건 근처에서 재검토하기로 유지

---

## 2026-09-09

### 뉴스 재수집 + 가격 매칭 + 라벨링 전량 완료
- 백필(20종목) 69,303 → 73,953건(+4,650), 큐레이션(삼성·SK하이닉스) 4,679 → 5,135건(+456) — 신규 수집분 반영
- 가격/지수 이력 갱신(9/8 종가까지 반영) 후 재매칭: 69,546건 매칭, 4,622건은 다음 거래일 종가 대기로 보류
- 라벨링 대상 재계산 결과 74,168건(신규 +4,657건 발생) → 657+1000+1000+1000+993건, 총 5배치로 **74,168/74,168건(100%) 완료**
- 최종 분포: 중립 46.7%(34,602) / 긍정 23.7%(17,595) / 무관 18.5%(13,742) / 부정 11.1%(8,215) / 파싱실패 14건 — 이전과 비율 거의 동일
- 매 배치 전 GPU 상태 확인, 이상 없음. 백업 3종 파일 동기화 완료

### 재학습(v7) 타이밍 판단
- v6 학습 데이터(62,972건) 대비 지금(74,168건)은 +17.8%뿐 — v5→v6에서 유의미했던 증가폭(+69%) 기준엔 한참 못 미침
- 최근 평일 신규 유입량(4,000~5,000건/일) 감안 시 +50%(약 94,000건) 도달까지 대략 일주일~열흘 예상. 그 지점 이후 재학습 재검토하기로 함

---

## 2026-09-08

### GPU 안전 규칙 확정
- 라벨링 배치 도중 다른(무관한) 프로젝트 프로세스가 GPU를 동시에 점유해 VRAM이 7563/8188MiB(92%)까지 치솟는 상황 발생 — 8/28·9/2에 실제 드라이버 크래시를 냈던 패턴과 동일해 즉시 작업 중단
- 이후 규칙 확정: ①GPU 작업 시작 전 항상 `nvidia-smi`로 체크 ②남의 프로세스가 물려있으면 대기(동시 실행 금지) ③내 작업만 있어도 시작 시점 VRAM이 30% 이상이면 시작 안 함. 메모리에 저장(`feedback_gpu_safety_rule.md`)
- `conda run`이 콘솔 출력을 cp949로 재인코딩하다 깨지는 버그 발견(UnicodeEncodeError) — GPU/라벨링과 무관, `unsloth_env/python.exe`를 직접 호출하는 방식으로 우회

### Qwen 라벨링 진행 — 전량 완료
- 500건 + 1000건 + 1000건 + 979건, 총 4배치로 65,524 → 69,511건(100%) 완료
- 최종 분포: 중립 46.6%(32,392) / 긍정 23.5%(16,359) / 무관 18.7%(13,003) / 부정 11.1%(7,743) / 파싱실패 14건 — v6 대비 비율 거의 동일
- 매 배치 전 GPU 상태 확인 후 진행, 이상 없음
- v6(62,972건) 대비 증가폭 +10.4%뿐이라 지금 재학습해도 개선 기대난 — 재학습은 데이터 더 쌓일 때까지 보류

### .gitignore 정리
- 조사용으로 만들었던 일회성 스크립트(`scripts/check_*.py`, `scripts/debug_*.py`)를 gitignore에 추가 — 재사용 목적 아닌 디버그 산출물이라 추적 대상에서 제외
- 실제 파이프라인 스크립트(`prep_all_news_for_labeling.py`, `rag_eval_*.py`)와 실패 기록용 `reclassify_sports_noise.py`는 그대로 추적 대상 유지

---

## 2026-09-07

### 일상 루틴 수집
- 백필 62,972 → 64,845건, 큐레이션(삼성전자·SK하이닉스) 4,041 → 4,237건
- 가격/지수 갱신 시도했으나 09-04(금) 이후 진전 없음 — 아직 이번 주 첫 거래일 종가가 데이터 소스에 미반영. 매칭은 57,137건 그대로(신규 매칭 없음), 보류분만 5,835 → 7,917건으로 증가
- 화요일 이후 재시도 예정 (다음 세션)

---

## 2026-09-06 (2)

### RAG 검색 품질 평가 실행
- 쿼리 80건 샘플 → 임베딩 검색 top-5 vs BM25(문자 bigram, 형태소 분석기 미설치) top-5 → Qwen이 (쿼리,후보) 800쌍 관련성 판정
- 1차 판정 실패: `관련성: 관련` 형식을 기대한 정규식이 실제로는 Qwen이 그냥 "관련"이라고만 답해서 800건 중 791건 파싱실패 → 정규식을 "관련"/"무관" 부분일치로 완화해서 해결
- **결과: Precision@5 — BM25 97.25% vs 임베딩 98.50%** (임베딩이 근소 우위)
- **중요한 캐비엇 발견**: 무작위 쿼리 중 KT 종목 쿼리 하나가 그 자체로 이미 노이즈(LG 트윈스 투수 기사가 KT로 오분류)였음 → 검색된 이웃들도 전부 같은 야구 노이즈라 "서로 관련 있다"고 판정됨. 즉 **판정 AI가 "쿼리 텍스트와 유사한가"를 볼 뿐 "진짜 그 종목 얘기인가"는 안 봄** — 검색 자체(임베딩)는 제 역할을 하지만, 라벨링 단계에서 새어든 오염이 코퍼스 안에서 끼리끼리 뭉쳐 있으면 이 지표가 못 잡아냄. 97~98%라는 숫자는 정확히 계산됐지만 이 한계를 감안해서 읽어야 함

### 스포츠단 노이즈 규칙 기반 재분류 — 시도했다가 되돌림
- 배경: KT(kt wiz)뿐 아니라 삼성전자(삼성 라이온즈)·기아(KIA 타이거즈)·한화에어로스페이스(한화 이글스)·LG계열(LG 트윈스) 전부 구조적으로 같은 문제에 노출됨을 확인
- 1차 시도(야구 용어 3개 이상): KT 457 + 기아 219 = 676건 재분류, 샘플 확인 결과 오탐 없어 보였음
- 검증 삼아 "승리투수"/"홈런" 등으로 잔여 데이터 재검색 → 171건 추가 발견, 게다가 **LCK e스포츠**(kt Rolster, 디플러스 기아)·**KLPGA 골프**까지 얽혀있음을 확인
- 2차 시도(strong/weak 2단계 용어 확장): 972건 추가 재분류 → 검증 중 **KB금융 100건이 전부 골프 대회 스폰서십 기사**(진짜 사업 관련 뉴스!)로 오분류됐음을 발견 — 회사가 실제로 스폰서하는 스포츠 뉴스는 노이즈가 아니라 정당한 PR 콘텐츠라는 걸 놓침
- 골프 용어 제거 후 3차 시도 → KB금융은 100→19로 줄었으나, **더 근본적인 문제 두 가지 추가 발견**:
  1. 단어 경계 없는 단순 문자열 포함 검사라 "그리드위즈"(전혀 다른 회사) 안의 "위즈", "ST1"(현대차 배송차량 모델명) 안의 "T1"처럼 **무관한 고유명사에 우연히 포함**되는 오탐
  2. "세이브"(야구 세이브 vs 게임 저장), "이닝"(야구 이닝 vs "트레이닝"의 뒷부분)처럼 **동음이의어·부분일치 오탐**
- **최종 결론: 규칙 기반 필터는 폐기, `news_qwen_labeled.csv`는 백업본(순수 Qwen 라벨링 결과)으로 복원**. 오탐 메커니즘이 계속 새로 발견돼서 화이트리스트/블랙리스트 방식으로는 안전하게 못 잡는다고 판단. 제대로 하려면 Qwen 같은 모델로 건별 판정(전체 코퍼스 대상 GPU 비용 필요)이 필요 — 지금은 비용 대비 실익이 낮아 보류
- 이 시행착오 자체가 "규칙 기반 노이즈 필터의 위험성"을 보여주는 유효한 기록이라 스크립트(`reclassify_sports_noise.py`)와 함께 보존

### 산출물
- `scripts/rag_eval_build_candidates.py`, `scripts/rag_eval_judge.py`, `scripts/rag_eval_metrics.py`
- `scripts/reclassify_sports_noise.py` (최종 미적용, 기록용 보존)
- `data/processed/rag_eval_judged.csv`, `data/raw/reclass_candidates_v3.csv`

### 다음 할 일
- [x] 포트폴리오에 Precision@5 결과 + "판정 AI의 사각지대" 캐비엇 반영
- [x] RAG 데모 스크린샷 그리드 1열 변경 (이미 적용돼 있었음 확인)
- [ ] (보류) 노이즈 필터링을 제대로 하려면: Qwen 재라벨링 시 프롬프트에 "스포츠단 게임 결과 보도는 무관"을 더 명시적으로/예시와 함께 추가하는 방향이 규칙 기반보다 안전할 수 있음

---

## 2026-09-06 (9/5~9/6에 걸친 작업)

### 라벨링-매칭 구조 분리 — 매칭 대기 없이 라벨링 가능하게 변경
- 문제의식: 논조 라벨링은 텍스트만 있으면 되는데, 지금 구조는 매칭 완료된 파일에서만 라벨링 입력을 만들고 있어서 "다음 거래일 가격이 없으면" 라벨링도 못 하는 불필요한 종속관계가 있었음
- `scripts/prep_all_news_for_labeling.py` 신규 작성: 매칭 여부와 무관하게 원본 뉴스(backfill+curated) 전체를 라벨링 입력으로 만듦
- `label_qwen_full.py`가 두 번째 인자로 입력 파일 경로를 받도록 수정 (기존 링크 기준 체크포인트/재시작 로직은 그대로 재사용됨)
- 이 구조로 매칭 보류 중이던 5,835건(주말 발행분 포함, 다음 거래일 가격이 아직 없던 것들)을 매칭 기다리지 않고 바로 라벨링 가능해짐

### 뉴스 수집 + 매칭 + 라벨링 전량 완료
- 백필 61,104 → 62,765건, 큐레이션 3,872 → 4,041건 (토요일 발행분도 정상 수집됨, 실제 확인 결과 주말 뉴스도 다음 거래일 기준으로 매칭에 문제없음을 재확인)
- 매칭: 57,137건 (원본 62,972건 중 5,835건은 여전히 "다음 거래일 가격 대기" 보류)
- **라벨링: 62,972/62,972건(100%) 완료** — 매칭 대기 중인 5,835건도 분리된 구조 덕에 전부 라벨링됨
- 최종 라벨 분포: 중립 46.7%(29,394) / 긍정 23.6%(14,884) / **무관 18.1%(11,392)** / 부정 11.6%(7,288) / 파싱실패 14건

### 다음 과제(재학습/임베딩/RAG평가) 소요시간 사전 테스트
- 임베딩 재구축(51,305건, 라벨링 도중 시점 기준): **약 2분 5초**
- split + 재학습(3 epoch): **약 15분 37초** (학습 자체 14분 41초) — `models/klue-bert-timing-test`에 임시 저장 후 삭제(테스트 목적, 정식 반영 아님)
- **부가 발견**: 테스트 재학습 결과 accuracy 84.8%/F1 83.7%로 기존 v5(83.8%/82.9%)보다 오히려 높게 나옴 — 데이터가 30,511→51,305건으로 늘어난 영향으로 추정. "이 구조는 데이터 늘려도 성능 정체"라던 기존 결론을 재검토할 만한 신호. 정식 재학습 시 재확인 필요

### 포트폴리오(`portfolio/sentiquant.html`) 추가 정비
- 스크린샷 그리드 3열 → **1열**로 변경, 고정 260px 크롭 제거하고 `height:auto`로 원본 비율 그대로 표시 (지난 세션에 방향만 정하고 미적용이었던 것 마저 적용)
- 상단 통계 카드 "수집 원본 뉴스" 46,970 → 62,972로 갱신 (README도 동일하게 동기화)
- "최종 학습 표본"(30,511건)과 "정확도"(83.8%)는 실제 학습된 v5 모델 기준이라 그대로 유지 — 원본 수집량이 늘었다고 자동으로 안 바뀜을 명확히 함
- "데이터 기간" 각주 추가: `2026.04.28 ~ 진행 중 (매일 수집 갱신)` — "작업 기간"(8/25~8/30)과는 별개 개념으로 분리 표기
- README 개인정보(이메일/GitHub) 관련 오갔다가 원복 — git 커밋 메타데이터에 이미 공개된 정보라 README에서만 지우는 게 의미 없다는 사용자 지적으로 정정

### 임베딩·재학습 정식 실행 — "성능 정체" 결론 재검토, v6로 확정
- 임베딩 정식 재구축: 51,566건(무관/파싱실패 제외 전량) → `data/processed/news_embeddings.npy`
- split + 재학습 정식 실행: train 36,096/val 7,735/test 7,735 → **`models/klue-bert-qwen-sentiment-v6`: test accuracy 85.05%, F1(macro) 83.63%**
- v5(83.8%/82.9%) 대비 확실한 개선. test 표본 7,735건 기준 표준오차 추정 약 0.4%p인데 실제 차이는 1.25%p로 노이즈 범위를 벗어남 — 통계적으로 유의미한 개선으로 판단
- **결론 정정**: v3→v4→v5 구간은 데이터가 24%(+5,816건)만 늘어서 정확도가 거의 안 움직였던 것뿐이었고, "이 아키텍처는 데이터를 늘려도 정체된다"는 결론은 성급했음. v5→v6처럼 69%(+21,055건) 늘리자 확실히 개선됨 — **정체가 아니라 "그동안의 증가폭이 충분히 크지 않았다"**로 결론 수정. `models/klue-bert-qwen-sentiment-v6`가 새 최종 모델
- 포트폴리오(정확도 추이 차트, 라벨 분포 차트, 상단 통계)와 README(핵심 결과 표, "이 프로젝트가 보여주는 것" 항목 추가) 전부 v6 기준으로 갱신

### 다음 할 일
- [ ] 재학습 후 매칭 보류였던 5,835건도 가격 데이터 들어오는 대로 주기적으로 재확인
- [ ] RAG 데모(`api/`, `rag_pipeline.py`)가 v6 모델과 새 임베딩을 가리키도록 갱신 확인
- [ ] RAG 검색 품질 체계적 평가 (Precision@5 등, 계속 이월 중인 과제)

### 산출물
- `scripts/prep_all_news_for_labeling.py`, `scripts/check_saturday_match.py`
- `data/processed/news_all_for_labeling.csv` (라벨링용, 매칭 무관 원본 전체)
- `data/processed/news_qwen_labeled.csv` (62,972건, 100% 완료)
- `data/processed/news_embeddings.npy`/`news_embeddings_meta.csv` (51,566건, 재구축)
- `models/klue-bert-qwen-sentiment-v6` (새 최종 모델)

---

## 2026-09-05 (9/4~9/5에 걸친 작업, 진행 중)

### 뉴스 수집 + 매칭 계속
- 백필 누적: 57,003건(9/4) → 61,104건(9/5), 큐레이션 3,872건
- 매칭 47,125 → 57,137건 (지수 결측 없이 깔끔하게 갱신 — `collect_index_history.py` 보간 로직이 잘 작동 중)

### GPU 경합 —
- 라벨링 도중 속도 저하(1.4건대 → 0.84건/초) → 확인해보니 이번엔 다른것이 실행 중이었음(GPU 61% 점유)
- 이벤트 로그 확인 결과 새로운 드라이버 크래시는 없음(9/2 그때 기록만 남아있음) — 순수 자원 경합으로 인한 속도 저하였고, 게임 종료 후 1.4건대로 정상 복귀 확인
- 다른 GPU 프로그램이 걸릴 때마다(다른딥러닝이던 게임이든) `nvidia-smi`+프로세스 확인으로 원인을 바로 특정하는 패턴이 자리잡음

### 라벨링 진행 (백로그 15,009건 → 3,985건, 미완료)
- 500~1000건 단위로 12회 실행
- **현재 상태(중단 시점, 늦은 시간이라 종료)**: 매칭 57,137건 / 라벨링 53,152건 / **남은 백로그 3,985건**
- 진행 중이던 큰 단위(3,985건 한 번에) 실행은 사용자 요청으로 중간에 강제 종료함 — 체크포인트가 1,000건 단위라 종료 시점 기준 마지막 체크포인트(53,152건)까지는 안전하게 저장됨, 유실 없음
- 다음 세션에서 이어서 진행: `python scripts/label_qwen_full.py <목표건수>` (입력 파일 `data/processed/news_labeled_excess_v8.csv` 이미 준비됨 — 뉴스를 더 수집했다면 v9로 새로 스냅샷 떠야 함)
- 백로그 다 처리되면 `split_qwen_labeled.py` → 재학습(v6) 여부 판단 (v4→v5 사례처럼 정확도 개선은 기대 안 하는 게 맞음, 최신 데이터 반영 여부만 판단)

## 2026-09-04 (9/3~9/4에 걸친 작업, 진행 중)

### 뉴스 수집 계속 (9/3, 9/4) + 라벨링 백로그 관리 교훈
- 백필 누적: 52,056건(9/3) → 57,003건(9/4), 큐레이션 3,431건
- **문제**: 9/2 새벽 라벨링을 36,792/36,792(백로그 0)까지 끝낸 뒤, 이후 며칠간 수집·매칭은 매일 루틴대로 돌렸지만 라벨링은 안 돌림 → 하루 ~5,000건씩 매칭 백로그가 쌓여서 9/4 기준 10,333건까지 불어남
- **교훈**: 수집·매칭은 자동으로 매일 쌓이는데 라벨링은 수동 트리거라, 며칠 밀리면 한 번에 몰려서 처리 부담이 커짐 → 앞으로는 라벨링도 자주(거의 매일) 돌리는 걸 권장
- **"계속 쌓을까" 재논의**: 저빈도 종목은 가끔 몰아서 수집해도 되지만, 고빈도 종목(삼성전자·카카오·현대차·KT·기아 등)은 NAVER API가 최근 N건만 노출하는 구조라 며칠만 걸러도 그 기간 뉴스가 영구 유실됨 — "생각날 때만 수집"은 비추천, 최소 3~4일 이내 주기로 가볍게(수집만이라도) 유지하는 게 맞다고 정리함

### 라벨링 진행 (백로그 10,333건 → 4,997건, 미완료)
- 197~1000건 단위로 9회 실행, 도중 GPU 컨디션 재확인(다른 GPU 프로그램 켜져 있어도 이번엔 VRAM 여유 있어서 크래시 없이 정상 속도 1.2~1.4건/초 유지됨 — 8/28~29 크래시 때와 달리 이번엔  VRAM을 덜 먹는 상태였던 것으로 추정)
- **현재 상태(중단 시점)**: 매칭 47,125건 / 라벨링 42,128건 / **남은 백로그 4,997건**
- 다음 세션에서 이어서 진행: `python scripts/label_qwen_full.py <목표건수>` (입력 파일은 `data/processed/news_labeled_excess_v7.csv`, 이미 준비되어 있음 — 새로 뉴스를 더 수집했다면 v8로 다시 스냅샷 떠야 함, DATA_PIPELINE_RULES.md 참고)
- 백로그 다 처리되면: `split_qwen_labeled.py` → `train_classifier.py`로 v6 재학습 여부는 그때 판단 (v4→v5 사례처럼 정확도 개선은 기대하지 않는 게 맞음, 그래도 최신 데이터 반영 차원에서 진행할지는 선택)

## 2026-09-02 (9/1~9/2에 걸친 작업)

### 뉴스 수집 계속 (9/1, 9/2)
- 매일 루틴대로 큐레이션(삼성전자·하이닉스) + 전종목 백필 실행
- 백필 누적: 41,903건(9/1) → 46,970건(9/2)

### yfinance 코스피 지수 8/28 결측 — 진짜 원인 찾음
- 9/1 새벽 최초 발견: 8/31 종가 전종목 NaN + 코스피 지수만 8/28이 통째로 빠짐 → 처음엔 "일시적 지연"으로 판단, 다음날 재시도하기로 함
- 9/2 재확인: 8/31은 정상 채워졌으나 **8/28 코스피 결측은 그대로 재현됨** — 재시도 3회(기본 재요청, 다음날 재요청, `yf.download`에 명시적 start/end 지정)해도 동일 → 일시적 문제가 아니라 Yahoo 소스 자체에서 그 날짜의 `^KS11` 데이터가 빠진 것으로 확정 (개별 종목엔 8/28 데이터가 정상적으로 존재함, 지수만 없음)
- **해결**: `collect_index_history.py`를 수정해, 개별 종목 가격(`price_history.csv`)의 실제 거래일 캘린더를 기준으로 지수 시계열을 reindex한 뒤 내부 결측만 선형보간(`interpolate(inside)`)하도록 함 — 최신일(아직 미발표)은 보간 대상에서 자동 제외됨(양쪽 끝점이 있어야 보간되므로). 앞으로 같은 종류의 고립된 단일일 결측이 생기면 자동으로 복구됨
- 이 버그 때문에 `add_excess_return.py`에서 결측치가 5,107건(8/27→8/28 매칭분) → 12,573건(8/28→8/31 매칭분 추가)까지 쌓였다가, 수정 후 0건으로 해결됨

### GPU 자원 경합으로 실제 드라이버 크래시 발생 — 원인 규명
- 라벨링 도중 속도가 평소(1.3~1.5건/초)보다 크게 느려짐(0.89건/초) + 사용자가 nvidia 오류 메시지 목격
- `nvidia-smi`로 확인해보니 **별도 서버가 VRAM ~3GB를 상시 점유한 채 백그라운드 실행 중**이었음
- Windows 이벤트 로그(`nvlddmkm`, Event ID 13/153)에서 라벨링 중이던 시각과 정확히 겹치는 **실제 GPU 드라이버 타임아웃/강제복구(TDR) 기록** 확인 — 단순 느려짐이 아니라 진짜 드라이버 크래시였음
- 원인: VRAM은 계속 점유됨 → Qwen이 필요로 하는 VRAM과 합쳐서 8GB 한도 근처에서 충돌
- 사용자가 종료 후 100건 스피드테스트(1.1~1.2건/초, 정상 범위) → 이후 청크들 1.4~1.56건/초로 완전 정상화 확인
- **교훈**: 다른 GPU 프로세스는 "지금 뭘 시키고 있냐"가 아니라 "모델이 로드되어 켜져 있냐"가 VRAM 점유 여부를 결정함 — 다음에 라벨링/학습 전에 `nvidia-smi`로 다른 프로세스 확인하는 걸 습관화할 것

### Qwen 라벨링 백로그 전량 처리 (7,466건)
- 800~1000건씩 9번에 나눠 실행, 전량 완료(36,792/36,792, 백로그 0)
- split 재생성: 30,511건(무관/파싱실패 제외, 이전 24,695건 대비 +5,816) — train 21,357 / val 4,577 / test 4,577

### 재학습 — v5, 정체 재확인
- `models/klue-bert-qwen-sentiment-v5`: **test accuracy 83.8%, F1(macro) 82.9%** — v4(83.5%/82.2%) 대비 거의 그대로
- 데이터 24,695→30,511건(+24%)에도 성능 정체 재확인 — 이 아키텍처의 한계라는 기존 결론이 또 한 번 검증됨. **v5가 현재 최종 모델**

### 디스크/백업 정리
- `backup/` 폴더 최신화(덮어쓰기): 세 파일 다 이어붙이기 방식이라 신버전이 구버전을 항상 포함 — 버전 보존할 실익 없다고 판단, 덮어쓰는 쪽으로 정리 (37MB → 47MB)
- `models/`: v4 삭제(v5로 대체됨) + v5의 `checkpoint-*` 서브폴더 삭제 → 다시 423MB로 정리
- `data/processed/news_labeled*.csv` 계열(구 주가기반 라벨 2개 + Qwen 라벨링용 임시 스냅샷 v2~v6, 총 7개 ~58MB) 전부 삭제 — 전부 `news_price_matched.csv`에서 그때그때 재생성 가능한 임시 파일들

### 남은 것 (다음에 할 일)
- [x] RAG 데모 임베딩(`news_embeddings.npy`) 30,511건으로 갱신 완료
- [ ] (기존 목록 계속) RAG 검색 품질 체계적 평가, 스크린샷 그리드 레이아웃, 배포 여부 결정

---

## 2026-08-31

### README를 외부(이력서 방문자)용으로 재작성, 내부 기록은 WORKLOG로 이관
기존 README.md는 "다음 세션이 이어갈 수 있도록"이라는 내부 인수인계 문서 톤이었음 — 이력서 보러 오는 사람 기준으로는 안 맞다고 판단, 완전히 새로 씀(핵심 결과 요약 + "이 프로젝트가 보여주는 것" + 시스템 구성 + 로컬 실행법). 아래는 기존 README에만 있던 내용 중 보존할 것들.

**모델 비교표 (최종: `models/klue-bert-qwen-sentiment-v4`)**

| 모델 | Test Accuracy | F1(macro) | 상태 |
|---|---|---|---|
| 주가 기반 라벨 (최고 기록) | 38.5% | 38.5% | 폐기, 참고용 |
| 논조 라벨 v1 (전환 직후, split 버그 있었음) | 81.3% | 79.3% | 폐기 |
| 논조 라벨 v3 (split 버그 수정: 날짜 기준 → (종목×라벨) stratified) | 83.0% | 82.0% | 폐기 |
| **논조 라벨 v4 (최종)** | **83.5%** | **82.2%** | **현재 최종 모델** |

v1→v3 사이 버그: 날짜 기준 split이 18종목 중 10종목을 val/test에서 완전히 누락시키고 있었음(81.3%가 사실상 상위 5종목 평균이었음) — (종목×라벨) 행 단위 stratified 방식으로 교체. 경위는 2026-08-28 항목 참고.

**스크립트 맵**

| 스크립트 | 역할 |
|---|---|
| `collect_multi_sample.py` | 20종목 리스트(`STOCKS` dict) 정의 + 소량 테스트 수집 |
| `collect_news_backfill.py` | 종목당 최대 1000건 뉴스 백필 (재실행 시 기존과 merge+dedup) |
| `collect_news_curated.py` | 삼성전자·SK하이닉스 전용 일일 소량 큐레이션 수집 |
| `collect_price_history.py` / `collect_index_history.py` | 종목/코스피 지수 1년치 주가 (매칭 전 항상 먼저 갱신) |
| `match_news_price.py` | 뉴스+주가 매칭 (backfill + curated 자동 병합) |
| `add_excess_return.py` | 코스피 대비 초과수익률 계산, `news_price_matched.csv`에 컬럼 추가 |
| `label_qwen_full.py` | Qwen3 논조 라벨링 (배치+체크포인트, 목표건수 인자로 짧게 끊어서 실행 가능) |
| `split_qwen_labeled.py` | Qwen 라벨에서 "무관" 제외 + (종목×라벨) stratified train/val/test 분할 |
| `train_classifier.py` | KLUE-BERT 파인튜닝 (데이터/출력 경로 인자로 받음) |
| `label_and_split.py`, `find_good_seed.py` | 주가 기반 라벨링 시절 스크립트 (현재 미사용, 참고용 보존) |
| `check_*.py` | QA/진단 유틸리티 (분포 확인, 갭 확인 등) |
| `pilot_qwen_labeling.py` | Qwen3 라벨링 소규모 파일럿 테스트용 |
| `build_news_embeddings.py` | 뉴스 전량을 `jhgan/ko-sroberta-multitask`로 임베딩 (1회성) |
| `rag_pipeline.py` | `SentimentRAG` 클래스 — 분류+검색+Qwen 설명을 하나로 묶음 |
| `api/main.py` | FastAPI 서버 (`/analyze`, `/explain`) |
| `web/` | Next.js UI |

**다음에 할 일 (우선순위 순, 2026-08-30 기준으로 확정된 목록)**
1. RAG 검색 품질 체계적 평가 — 지금은 수작업 스팟체크 수준. 쿼리 기사 50~100건 평가셋 구성 → Precision@5 등 지표화 → BM25 키워드 검색 대비 비교
2. RAG 데모 스크린샷 그리드(`portfolio/sentiquant.html` 06번 섹션)를 3열 → 1열로 레이아웃 변경 (방향만 정함, 미적용)
3. 네이버 뉴스 링크 스크래퍼 추가(선택), UI 스트리밍/병렬화, 배포 여부 결정
4. (참고, 착수 안 함) 뉴스 외 주식 커뮤니티 텍스트까지 학습 확장 — 공식 API 없어 스크래핑 회귀 필요, 노이즈 심함. 대략 3~5일 예상
5. "논조-수익률 효과크기가 작다"는 결론 자체는 개선 대상 아님(세 번의 분석에서 r=0.04~0.13으로 수렴 확인, 억지로 키우면 p-hacking) — 종목/섹터별 세분화나 논조 강도 기반 재분석 등 더 정교하게 파고드는 방향은 여지 있음

### 디스크 정리 — models/ 33GB → 423MB
- 원인: `Trainer` 자동 체크포인트(`checkpoint-N`)가 옵티마이저 상태까지 통째로 저장 — 모델당 체크포인트 3개 × 1.3GB, 실제 필요한 최종 파일(safetensors+config)은 424MB뿐이었음
- v4 제외 구모델 전체(`klue-bert-return`, `-excess`, `-excess-v2`, `-sentiment`, `-qwen-sentiment` v1~v3) 삭제 — 수치는 위 비교표에 이미 보존됨
- v4의 `checkpoint-*` 서브폴더도 삭제, 실제 사용 파일만 남김

### 백업 폴더 구성 — "복구 불가능한 것만" 기준
`backup/` 폴더에 아래 3개만 복사(총 37MB). 기준: NAVER API가 최신 데이터만 보여줘서 지난 뉴스는 재수집 불가능하거나, 재생성에 GPU 시간이 크게 드는 것만 선정. 가격 데이터(yfinance로 언제든 재수집 가능), 파생 CSV(스크립트로 몇 분이면 재생성), 모델 가중치(재학습 7분)는 제외.
- `data/raw/news_backfill.csv` (17MB)
- `data/raw/news_daily_curated.csv` (844KB)
- `data/processed/news_qwen_labeled.csv` (20MB, Qwen 20시간+ GPU 라벨링 결과물)

### .gitignore 정리
`web/node_modules/`, `web/.next/`(Next.js 생성물), `Entropy645_포트폴리오.html`(다른 프로젝트 파일, 실수로 섞여 있었음) 추가. 이후 사용자가 직접 `img/`, `portfolio/`도 추가함(개인 사진·이메일이 담긴 포트폴리오 HTML과 스크린샷을 공개 저장소에는 안 올리기로 결정한 것으로 보임).

## 2026-08-30

### 포트폴리오 페이지(`portfolio/sentiquant.html`) 버그 수정
- "주가 기반 라벨 — 실험 3회" 막대그래프가 안 보이는 문제 → `<rect>`의 `height`에 음수값(`-56`/`-70`/`-77`)이 들어가 있어서 SVG가 렌더링을 거부한 게 원인. 양수 height + 정확한 y좌표로 수정
- 소제목(`<h2>`)들이 전부 의도한 2줄이 아니라 3줄로 꺾이는 문제 → 공통 CSS `.section-head h2{ max-width:20ch }`가 원인. `ch`는 한글엔 너무 좁은 기준(한글 1자가 라틴 문자보다 훨씬 넓음)이라 `<br>`로 나눈 각 줄이 또 한 번 자동 줄바꿈됐던 것. `max-width` 제거로 전체 일괄 해결
- 05→06 섹션 전환이 끊겨 보이는 문제(왜 RAG로 방향을 틀었는지 설명 없이 바로 넘어감) → "이 정도 신호로 확정적 예측을 내세우는 건 정직하지 않다고 판단했다..." 전환 문장 추가
- RAG 데모 스크린샷 3장이 3열 그리드 + `object-fit:cover` 고정높이(260px)에 눌려 과도하게 잘림(`기본화면.jpg`는 가로 60% 가까이 손실) → 1차로 클릭 시 원본 비율 전체화면 라이트박스(이전/다음/ESC 지원) 추가. 다만 "포트폴리오는 클릭 안 해도 스크롤만으로 파악돼야 한다"는 논의 끝에 **기본 썸네일 자체를 1열로 바꿔 안 잘리게 하는 게 맞다는 결론 — 아직 미적용, 다음에 마저 할 것**

### 논의만 하고 실행 안 한 것 (기록용)
- 뉴스 외 **주식 커뮤니티 텍스트**(종목토론실 등)까지 학습 확장 아이디어 → 공식 API 없어 스크래핑 회귀 필요, 노이즈 훨씬 심함, 약관 회색지대 등으로 뉴스보다 확실히 어려움. 대략 3~5일 예상(스크래퍼 작성 1일 + 수집 1일 + Qwen 프롬프트 재검증 0.5~1일 + 라벨링·재학습 반나절~1일). **착수 안 함, 아이디어만 기록**
- "한계" 섹션 두 항목 검토:
  - RAG 검색 품질(수작업 스팟체크 수준) → **개선 가능**, 다음 과제로 채택 (아래)
  - 논조-수익률 효과크기(r²<2%) → 이미 세 번의 분석에서 같은 좁은 범위(r=0.04~0.13)로 수렴 확인된 정직한 결론. 숫자를 키우려는 시도는 p-hacking에 해당해서 **"개선 대상 아님"으로 확정** — 다만 종목/섹터별 세분화나 논조 강도 기반 재분석 등 "더 정교하게 파고드는" 방향은 여지 있음

### 다음 할 일
- [ ] **RAG 검색 품질 체계적 평가** (다음 과제로 확정): 쿼리 기사 50~100건 평가셋 구성 → Precision@5 등 지표화 → BM25 키워드 검색 대비 비교
- [ ] RAG 데모 스크린샷 그리드 1열로 레이아웃 변경 (위에서 방향만 정하고 미적용)
- [ ] (이전 목록 계속) 네이버 뉴스 링크 스크래퍼 추가(선택), UI 스트리밍/병렬화, 배포 여부 결정

---

## 2026-08-29

### 논조-수익률 역상관 원인 분석 — 다중 시차(t+1/3/5/10) 검증
- t+1에서만 보고 "역상관"이라 결론짓기엔 성급하다는 문제의식 → 시차를 늘려가며 재검증
- 동일 코호트(t+10까지 데이터 있는 2,949건, 표본 구성 편향 제거 위해 고정) 기준:
  - t+1: r=-0.066 (역상관 유지)
  - t+3: r=+0.080 (부호 반전)
  - t+5: r=+0.089
  - t+10: r=+0.078 (양의 방향 유지)
- **결론**: 다음날(t+1)만 보면 과잉반응/역인과로 역상관처럼 보이지만, 3거래일 이후부터는 논조가 기대 방향(긍정↔상대적 호실적)대로 자리잡아 t+10까지 유지됨. 단기 과잉반응 후 되돌림 + 중기 드리프트 패턴과 부합. r 자체는 여전히 작아(설명력 <1%) "매매전략"보다는 "논조에 정보가 있으나 반영에 며칠 걸린다"는 통계적 증거 수준
- **한계**: 이 고정 코호트가 저빈도 종목(POSCO홀딩스·신한지주·LG화학 등) 위주로 편향됨 — 고빈도 종목(삼성전자·카카오 등)까지 일반화하려면 시간이 더 지나서 걔들도 t+10 데이터가 쌓여야 확인 가능
- 산출물: `data/processed/news_multi_horizon.csv`

### RAG 데모 구현 — 논조 분류 + 유사사례 검색 + Qwen 설명
- 프로젝트 방향 논의: "논조-주가 상관관계"는 효과크기가 작아 데이터를 더 모아도 안 커진다는 결론(오늘 앞선 분석들의 일관된 r 범위 0.04~0.13 근거) → "모델 성능 극대화"보다 "정직한 파이프라인 완성 + RAG로 확장"에 집중하는 방향으로 정리
- 재활용성 점검: klue-bert-v4를 일반 커뮤니티 텍스트(반어법·은어)에 적용 → 실패(전부 "중립"으로 회피, 확신도 낮음). 반면 애널리스트 리포트·전자공시 스타일에는 잘 작동(형용사 없는 건조한 공시문도 정확히 판단) — "격식체냐"가 아니라 "학습 도메인(금융뉴스)과 같은 종류냐"가 기준이었음이 확인됨
- **RAG 파이프라인 구축**:
  1. `scripts/build_news_embeddings.py`: `jhgan/ko-sroberta-multitask`로 뉴스 24,695건 임베딩 (50초 소요)
  2. `scripts/rag_pipeline.py`: `SentimentRAG` 클래스 — klue-bert 즉시분류 + 코사인 유사도 검색(numpy) + 유사 과거사례의 실제 t+1~t+10 초과수익률 조회 + (지연로딩) Qwen3-4B 설명 생성
  3. `api/main.py`: FastAPI, `/analyze`(즉시) · `/explain`(Qwen, 첫 호출만 느림 ~32초, 이후 ~12초) 분리 — Qwen을 항상 띄워두지 않고 "추가 분석" 버튼 눌렀을 때만 지연 로딩하는 설계로 VRAM 절약(klue-bert+임베딩+Qwen 동시 로드 시 5.4GB/8GB, 여유 있음)
  4. `web/`: Next.js 16 최소 UI — 텍스트 입력 → 즉시 분류+유사사례 표 → "추가 분석" 버튼으로 Qwen 설명
- 설계 원칙: 링크가 아닌 텍스트 입력만 지원(스크래퍼 미구현, 의도적 범위 제한), Qwen 응답에도 "상관관계 약함/참고용" 문구를 프롬프트로 강제해서 과대해석 방지
- 로컬에서 end-to-end 테스트 완료 (`http://localhost:3000` ↔ FastAPI `:8000`)

### 다음 할 일 (RAG 데모)
- [ ] 네이버 뉴스 링크 스크래퍼 추가 (선택)
- [ ] UI 다듬기, "즉시결과 먼저 → Qwen 설명 나중" 스트리밍/병렬화
- [ ] 배포 여부 결정, README에 전체 서사 정리

### 역인과관계 검증 — 약한 신호 확인 (과대해석 주의)
- 가설: "부정" 라벨이 미래 하락을 예측하는 게 아니라, 이미 하락한 주가를 설명하는 기사에 붙는 꼬리표일 수 있다
- 발행일 기준 이전 t-1/t-3/t-5 거래일 초과수익률 vs 논조 라벨 상관관계 검증 (표본 24,695건):
  - t-1: r=+0.123 (지금까지 본 상관계수 중 제일 큼), 부정=-1.50% / 중립=-0.36% / 긍정=+0.16%
  - t-3: r=+0.039, t-5: r=+0.060 — 전부 방향 일치(부정<중립<긍정)
- **결론(수정)**: r=0.123은 Cohen 기준으로 "작은 효과" 구간 초입(r²≈1.5%)일 뿐, "강하게 확인됨"이라 표현한 건 과장이었음. "역인과관계일 가능성을 시사하는 약한 통계적 신호" 정도가 정확한 표현. "이미 하락한 뒤에야 기사가 나온다"는 단정도 무리 — 일별 종가 데이터만으로는 (1)기사가 가격을 뒤쫓는 경우와 (2)제3의 사건이 가격·기사 둘 다에 며칠에 걸쳐 영향을 주는 경우를 구분할 수 없음
- t+3~t+10 드리프트에 사전 모멘텀이 얼마나 섞였는지는 여전히 복합요인 분석(사전 모멘텀 통제) 없이는 확정 불가 — 효과크기가 작아서 우선순위 낮게 보류 중
- 산출물: `data/processed/news_pre_news_return.csv`

### v1 최종 마무리
- 20종목 전체 재백필(4,184건 회수) → 가격/지수 갱신(8/28 종가까지) → 매칭 29,326건 → Qwen 라벨링 백로그(8,074건) 전량 처리 → split 재생성(24,695건, train 17,286/val 3,704/test 3,705)
- `label_qwen_full.py` BATCH_SIZE 16 실험 → 오히려 느려짐(1.1건/초 vs 배치8의 1.3~1.5건/초, 프롬프트 길이 편차로 인한 패딩 낭비 추정) → 8로 원복
- 최종 재학습: `models/klue-bert-qwen-sentiment-v4`, **test accuracy 83.5%, F1(macro) 82.2%** — v3(83.0%/82.0%) 대비 거의 그대로, 이 아키텍처(klue-bert-base 단일분류)의 성능 한계에 도달한 것으로 결론. **v1 최종 모델로 확정.**
- 어제 논조-수익률 역상관 발견(긍정 -1.13% vs 부정 +0.86%)은 다음 세션에서 원인 분석 이어갈 것

## 2026-08-28

### 삼성전자·SK하이닉스 파이프라인 편입 + split 방법론 버그 수정
- 삼성전자·SK하이닉스 큐레이션 데이터 매칭(2,852건) → 전체 라벨링 파이프라인에 합류
- **종목별 test 표본 점검 중 큰 버그 발견**: 기존 `split_qwen_labeled.py`가 "날짜" 단위로 70/15/15 분할하는 방식이라, 18종목 중 10종목(카카오·KB금융·KT·LG에너지솔루션·NAVER·SK텔레콤·기아·삼성물산·한화에어로스페이스·현대차)이 val/test에 단 한 건도 없이 전부 train에만 존재했음. 81.3% 정확도는 사실상 8종목(그중 5종목이 96.6%)만으로 측정된 값이었음.
- **원인**: 날짜 기준 split은 주가 기반 라벨 시절 "동일 날짜 시장 전체 움직임 leakage 방지"용 설계였는데, Qwen 논조 라벨은 텍스트 자체 속성이라 그 우려가 무의미함. → **(종목 x 라벨) 기준 행 단위 stratified split으로 교체**
- 가격/지수 갱신 + 매칭 재실행 → 전체 20종목 21,252건 매칭, 이 중 7,212건이 "가격매칭은 됐지만 Qwen 라벨링은 안 됨" 상태로 밀려있던 것 추가 발견 (v2/v3 스냅샷이 예전 시점에 얼어있었던 게 원인) → 전부 라벨링 완료(21,252/21,252)
- 최종 재학습(`models/klue-bert-qwen-sentiment-v3`, 18,196건): **test accuracy 83.0%, F1(macro) 82.0%**
- 종목별 정확도 스프레드가 이전(73.3~92.0%, 18.7%p)에서 지금(78.6~87.9%, 9.3%p)으로 절반 가까이 줄어듦 — 이전에 "약점"으로 보였던 신한지주(75.4%)가 사실 표본 부족발 노이즈였고, 지금은 80.0%로 평균 근처임이 드러남

### 종목별 뉴스 수집 빈도 분석
- 백필이 API 1000건 한도에 걸려서, 하루 발행량 많은 종목일수록 커버 기간이 짧아짐을 발견 (삼성전자 663건/일→3일 커버, POSCO 7.7건/일→120일 커버)
- 카카오(345/일)·현대차(245/일)·KT(242/일)·기아(182/일)·SK텔레콤(147/일)·삼성물산(96/일)이 하이닉스급 고빈도였는데 일일 수집 루틴이 없어서 놓칠 뻔함
- 전체 재백필 실행 → 6,354건 신규 회수 (카카오 680, 기아 369 등). "며칠 안에 다시 안 돌렸으면 API에서 영구 유실될 뻔한" 분량이었음
- `DATA_PIPELINE_RULES.md` 신설: 파이프라인 순서, 종목별 수집주기 티어, 재학습 전 체크리스트, 파일 버전 메모 정리

### 뉴스 논조 vs 실제 주가 상관관계 1차 분석 (핵심 가설 검증)
- 표본 18,196건, 논조별 초과수익률(KOSPI 대비, 다음 거래일): 부정 +0.86% > 중립 -0.08% > **긍정 -1.13%**
- 논조(순서형 -1/0/1) vs 초과수익률 피어슨 상관계수 **r=-0.169, p<0.0001** — "긍정 뉴스일수록 다음날 초과수익률이 낮다"는, 순진한 가설과 반대 방향의 통계적으로 매우 유의한 신호
- **해석은 아직 미확정**: (1) 이미 주가에 선반영된 뒤 차익실현, (2) 역인과관계(주가 하락을 설명하는 기사가 부정으로 라벨되고 다음날 반등), (3) 매칭 로직상 단기 과잉반응 되돌림을 포착했을 가능성 — 다음 세션에서 원인 구분 분석 필요

### 다음 할 일
- [ ] 논조-수익률 역상관 원인 규명 (섹터별/시차별 분석, 당일 vs 익일 수익률 비교 등)
- [ ] 내일: 전체 20종목 백필 재실행 → 가격/지수 갱신 → 매칭 → 라벨링 백로그 정리 (매칭 vs 라벨링 갯수 갭 체크 필수, `DATA_PIPELINE_RULES.md` 참고)
- [ ] 데이터 ~22,000~25,000건 정도 쌓이면 재학습 1회 더 (v1 최종 후보)
- [ ] 이후 방향: 데이터 양보다 "논조-주가 상관관계 분석 심화" 및 README/스토리텔링(로드맵 Phase 9)에 집중 권장

### 산출물
- `DATA_PIPELINE_RULES.md` (신규)
- `models/klue-bert-qwen-sentiment-v2`, `models/klue-bert-qwen-sentiment-v3` (현재 최종)
- `data/processed/news_sentiment_final.csv` (18,196건, 종목 stratified split)
- `scripts/split_qwen_labeled.py` (날짜 기준 → 종목x라벨 기준으로 교체)

---

## 2026-08-27

### Qwen3-4B 논조 라벨링 완료
- 남은 3,996건을 `label_qwen_full.py`로 목표건수 단위(1000/1200/596/596/296/300건)로 끊어서 이어서 실행, **전체 11,188건 라벨링 완료**
- 최종 분포: 중립 5,017건(44.8%) / 긍정 3,172건(28.4%) / 부정 1,661건(14.8%) / **무관 1,333건(11.9%)** / 파싱 실패 5건(0.04%)
- 결과 파일: `data/processed/news_qwen_labeled.csv` (11,188행)

### 논조 라벨로 KLUE-BERT 재학습 — 대성공
- "무관"/파싱실패 제외 → 9,850건, `split_qwen_labeled.py`로 날짜 그룹+랜덤셔플 split (train 8,589/val 522/test 739, 최적 시드 75)
- `train_classifier.py`의 `LABEL2ID`를 부정/중립/긍정으로 교체 후 재학습 → **test accuracy 81.3%, F1(macro) 79.3%**
- 혼동행렬: 부정 회수율 73.3% / 중립 80.8% / 긍정 86.7% — 세 클래스 모두 균형 있게 구분, 한쪽으로 쏠리는 패턴 완전히 사라짐
- **비교**: 주가 기반 최고 기록(38.5%) 대비 두 배 이상. "과제 재정의"가 가설대로 완전히 유효했음이 실증됨
- 결과: `data/processed/news_sentiment_final.csv`, `models/klue-bert-qwen-sentiment`
- 삼성전자·SK하이닉스 일일 큐레이션 누적 404건 (앞으로 3~5일치면 다음 재학습에 포함시키기 충분 — 논조 라벨은 날짜 다양성이 굳이 필요 없어서 예전보다 훨씬 빨리 쓸 수 있게 됨)

### 다음 할 일
- [ ] "논조 예측 vs 실제 주가" 사후 상관관계 분석 설계 (긍정 판정 기사들이 실제로 주가와 상관있는지)
- [ ] (병행) 삼성전자·SK하이닉스 일일 큐레이션 계속, 3~5일 쌓이면 라벨링 후 재학습에 포함
- [ ] Phase 4(Qwen3-4B LoRA 파인튜닝) 또는 Phase 5+(배포/대시보드)로 진행 여부 논의

---

## 2026-08-26

### Phase 3 — KLUE-BERT 파인튜닝 1차 시도 (주가 기반 라벨)
- 8,140건(원래 수익률, ±1.0% 임계값)으로 학습 → **test accuracy 25.7~31.7%, F1(macro) 25.8~30.3%** (찍기 확률 33.3%보다 낮거나 비슷)
- 혼동행렬 확인 결과 모델이 특정 클래스(상승 또는 중립)로 쏠려 도망가는 패턴, 실질적 신호 학습 실패
- **원인 진단**: (1) 헤드라인 텍스트만으로 익일 주가 방향을 예측하는 과제 자체가 원래 어려움(효율적 시장 가설과 관련), (2) 시장 전체가 오르내리는 날엔 무관한 기사도 다 같은 라벨이 붙는 라벨 노이즈

### 개선 시도 1 — 시장 대비 초과수익률 라벨링
- 코스피 지수(^KS11) 수익률을 종목 수익률에서 빼서 "초과수익률" 계산 (`scripts/add_excess_return.py`)
- 재학습 결과: accuracy 35.0%, F1 32.2% — 찍기 확률 첫 돌파, 다만 혼동행렬 보면 "중립"으로 쏠리는 패턴 여전
- **Split 시행착오 추가 발견**: 날짜가 111개뿐이라 무작위 셔플이라도 시드에 따라 라벨 분포가 크게 출렁임 → 여러 시드 탐색 후 최적 시드 채택 (`scripts/find_good_seed.py`)

### 개선 시도 2 — 데이터 확장
- 18개 종목 백필 재실행(15,877→21,286건), 주가/지수 데이터 갱신, 보류됐던 매칭 재시도, 삼성전자·SK하이닉스 큐레이션 데이터(112건) 매칭 파이프라인에 연결
- 매칭 결과 8,140→11,188건으로 확장
- 재학습 결과: **accuracy 38.5%, F1(macro) 38.5%** — 지금까지 최고 성능. 혼동행렬도 한 클래스로 쏠리지 않고 세 방향 다 어느 정도 구분 (하락 회수율만 약함, 24%)

### 방향 전환 논의 — "뉴스만으로 주가 예측"의 한계 인정
- 사용자가 "기사만으로 주가 예측은 과도한 기대 아니냐"는 의문 제기 → 타당한 지적으로 판단
- 대안: 라벨을 "주가 변동"이 아니라 "기사 자체의 논조(긍정/부정/중립)"로 바꾸고, 주가는 학습 후 별도 검증 단계로 이동
- 논조 라벨은 Qwen3-4B로 자동 생성하기로 결정 (사람이 대량 수작업 라벨링 어려움)

### Qwen3-4B 자동 라벨링 파일럿
- 1차 테스트: `max_new_tokens=80`이 너무 짧아 Qwen3의 "생각 모드"(`<think>`)에 응답이 전부 잘림 → `enable_thinking=False`로 해결
- 2차 테스트(25건, 3분류): 대체로 합리적이나 **무관 기사(예: "KT" 검색에 kt wiz 야구팀 기사가 걸림)를 못 거름**
- 3차 테스트(25건, 4분류로 "무관" 옵션 추가): 무관 기사 5/5 정확히 걸러냄, 노이즈 필터링 문제 해결 확인
- 속도: 건당 3.7초(비배치) → 배치 처리(`BATCH_SIZE=8`)로 초당 1.3~1.5건까지 개선 (약 5배)
- 전체 11,188건 예상 시간 약 2시간 → 10분/지정 건수 단위로 끊어서 체크포인트 저장하며 진행하는 방식 채택 (`scripts/label_qwen_full.py`, 링크 기준 재시작 가능)
- 오늘 최종 4,000/11,188건(35.8%) 완료 (10분 타임박스 1회 + 목표건수 지정 3회, 총 4번 실행)
  - 라벨 분포(3,000건 시점): 중립 41.2% / **무관 22.7%** / 긍정 19.3% / 부정 16.8%, 파싱 실패 0건
  - **무관 22.7%라는 수치로, 기존에 우려했던 키워드 검색 노이즈 문제가 실제로 상당했음을 정량적으로 확인**
- 실행 시간 실측(스크립트 자체 기준, 모델 로딩 포함): 880건/10.2분, 1,120건/14.6분, 1,000건/12.4분, 1,000건/약 12~13분 — 평균 초당 1.3건 내외로 안정적
- 사용자가 컴퓨터를 켜놓고 자고 싶지 않다고 해서, 매번 목표 건수를 지정해 짧게 끊어 실행하고 다음 진행 여부를 묻는 방식으로 운영 중 (체크포인트가 링크 기준이라 언제 끊어도 안전)
- GPU 발열 확인(사용자 우려로 실측): 유휴 43~45도, 라벨링 작업 중 61~63도에서 안정 (4060 Ti 안전 한계 88~90도 대비 여유 있음, 전력 105~130W/160W). 문제 없음으로 확인

### 1차 결과 (2026-08-26 세션 종료 시점, 7,192/11,188건, 64.3%)
| 라벨 | 건수 | 비율 |
|---|---|---|
| 중립 | 3,312 | 46.1% |
| 긍정 | 1,800 | 25.0% |
| 부정 | 1,045 | 14.5% |
| 무관 | 1,030 | 14.3% |
| 파싱 실패 | 5 | 0.07% |

3,000건 시점(무관 22.7%)보다 후반부 무관 비율이 낮아짐(14.3%) — 데이터 순서상 뒤쪽 구간이 상대적으로 노이즈가 적었던 것으로 추정. 전체 완료 후 최종 분포는 달라질 수 있음.

### 다음 할 일
- [ ] Qwen 라벨링 이어서 진행 (남은 3,996건) — `python scripts\label_qwen_full.py <목표건수>`
- [ ] 전체 완료 후: "무관" 제외하고 긍정/부정/중립 3분류로 KLUE-BERT 재학습, 성능 비교
- [ ] 재학습 후 "주가 검증 단계" 설계: Qwen이 긍정 판정한 기사들이 실제로 주가와 상관있는지 사후 분석
- [ ] (병행) 삼성전자·SK하이닉스 일일 큐레이션 계속, 보류 매칭 주기적 재시도

### 산출물
- `scripts/add_excess_return.py`, `scripts/find_good_seed.py`, `scripts/train_classifier.py`(경로 인자화)
- `scripts/pilot_qwen_labeling.py`, `scripts/label_qwen_full.py`, `scripts/check_qwen_progress.py`
- `data/processed/news_labeled_excess_v2.csv` (11,188건, 주가 기반 라벨)
- `data/processed/news_qwen_labeled.csv` (진행 중, 3,000/11,188건 논조 라벨)
- `models/klue-bert-return`, `models/klue-bert-excess`, `models/klue-bert-excess-v2` (실험별 체크포인트, git 미포함)

## 2026-08-25

### Phase 1 마무리 — 백필 + 뉴스-주가 매칭
- 20종목 각각 최대 1000건(API 한도) 백필 시도 → 첫 실행에서 종목별 편차 발견 (453~998건). 재조사 결과 진짜 원인은 **페이지네이션 드리프트**: 여러 페이지를 순차 호출하는 동안 실시간으로 새 기사가 계속 올라와서, 날짜순 정렬 결과가 호출 사이에 밀리며 중복/누락이 생김. 버그도 콘텐츠 한계도 아니라 자연스러운 현상으로 결론, 재시도 로직 추가 후 최종 15,877건(dedup) 확보
- 주가 데이터를 1개월 → **1년치로 재수집** (뉴스가 4월 말까지 있어서 커버 필요)
- 뉴스-주가 매칭 로직 구현: 뉴스 발행일 → **다음 거래일** 종가 변동률(전일 대비 %) 계산 (`scripts/match_news_price.py`)
  - 8,140건 매칭 성공, 7,737건은 최근(08-21~08-25) 뉴스라 다음 거래일 주가가 아직 데이터 소스에 안 올라와서(NaN) 매칭 보류 — 원본은 보존되며 며칠 뒤 재실행하면 자동 회수됨
  - 검증: 수동 계산(LG에너지솔루션 08-20→08-21 -4.05%)과 대조해서 로직 정확성 확인
- 삼성전자·SK하이닉스처럼 뉴스량이 너무 많아 1000건이 반나절 만에 소진되는 종목 대응: 하루 소량(80건)씩 제목 유사도 기반 중복 제거 + 언론사 쏠림 방지하며 큐레이션하는 `scripts/collect_news_curated.py` 제작 (매일 수동 실행 예정, 사용자 담당)

### Phase 2 — 라벨링 + Split
- 임계값 실험: 데이터 실제 분포(평균 +0.28%, 표준편차 3.29%) 기준으로 여러 후보 비교 → **±1.0%**가 우연히도 하락/중립/상승을 32.6/33.6/33.8%로 거의 완벽하게 3등분함, 이걸로 채택
- Train/val/test 분할에서 시행착오:
  1. 날짜 개수 기준 70/15/15 → 최근 날짜에 뉴스량이 쏠려서 실제 건수는 test 83% vs train 9%로 뒤집힘
  2. 건수 기준 블록 분할로 수정 → 블록 생성 로직 버그로 크기 왜곡
  3. 날짜 단위 7일 주기 라운드로빈으로 단순화 → **요일 효과**로 추정되는 라벨 쏠림 재발견 (test 하락 72%, val 상승 63%)
  4. 최종: 날짜를 고정 시드로 **랜덤 셔플 후 70/15/15 분할** (그룹 단위는 날짜 유지 → 같은 날 중복 기사가 train/test에 동시에 들어가는 누출은 방지) → train 5,457 / val 2,008 / test 675, 세 split 모두 합리적으로 균형
- 결과: `data/processed/news_labeled.csv` (label, split 컬럼 포함)

### 다음 할 일
- [ ] EDA (라벨 분포, 종목별/섹터별 시각화) — 선택 사항, Phase 3 진행에 필수는 아님
- [ ] 며칠 후: `collect_price_history.py` → `match_news_price.py` 재실행해서 보류된 7,737건 회수
- [ ] 삼성전자·SK하이닉스: `collect_news_curated.py` 매일 실행해서 누적
- [ ] Phase 3: KLUE-BERT/FinBERT-kr 파인튜닝 (예상: GPU 학습 자체 10~30분, 전체 작업 1~3시간)

### 산출물
- `scripts/collect_price_history.py`, `scripts/match_news_price.py`, `scripts/collect_news_curated.py`, `scripts/label_and_split.py`
- `scripts/check_backfill_range.py`, `scripts/check_match_gap.py`, `scripts/check_matched_stats.py`, `scripts/check_thresholds.py` (QA용 유틸리티)
- `data/raw/news_backfill.csv` (15,877건), `data/raw/price_history.csv` (1년치)
- `data/processed/news_price_matched.csv` (8,140건), `data/processed/news_labeled.csv` (최종 라벨+split)

---

## 2026-08-23

### Phase 0 — 환경 세팅 완료
- Conda 환경 `unsloth_env` (Python 3.12.13) 생성
- PyTorch 2.11.0+cu130 설치, RTX 4060 Ti GPU 인식 확인
- unsloth 2026.8.19 + transformers/peft/trl/bitsandbytes/accelerate/datasets 설치
- Qwen3-4B 4bit 로드 테스트 성공 (`scripts/test_qwen_load.py`)
- **이슈**: `pip install unsloth`가 index-url 없이 실행되면서 GPU용 torch를 CPU 빌드로 덮어씀 → cu130 인덱스로 강제 재설치해서 해결. 이후 패키지 추가 설치 시 GPU 인식 재확인 필요.

### Phase 1 — 데이터 수집 맛보기 테스트
- yfinance로 주가 수집 파이프라인 검증 (문제없음)
- 뉴스 수집 소스 탐색:
  - 네이버 금융 종목뉴스 위젯(`finance.naver.com/item/news_news.naver`) — 개편으로 더 이상 작동 안 함
  - Google News RSS — 작동은 하나 노이즈(무관 언론사, 리다이렉트 링크) 있음
  - **네이버 뉴스검색 API** — developers.naver.com에서는 신규 등록 불가 (`"신규로 등록할 수 없는 API가 선택되었습니다"` 에러). 확인 결과 검색 API가 **네이버클라우드플랫폼(NCP)의 "NAVER API HUB"로 이전**됨. `console.ncloud.com`에서 신청, 엔드포인트 `https://naverapihub.apigw.ntruss.com/search/v1/news`, 헤더 `X-NCP-APIGW-API-KEY-ID` / `X-NCP-APIGW-API-KEY`로 인증 방식 변경됨
- 최종적으로 네이버 뉴스검색 API 연동 성공, `.env`에 키 저장 (`NAVER_API_KEY_ID`, `NAVER_API_KEY`)
- 20종목(7개 섹터 분산: 반도체/2차전지/자동차/플랫폼/바이오/금융/화학에너지/철강조선/통신/유통소비재)으로 뉴스 400건 + 주가 23일치 수집 테스트 완료
- **알려진 이슈**: 종목명 단순 키워드 검색이라 무관 기사(연예 뉴스 등)가 섞임 → Phase 2 라벨링 전 필터링 필요

### 다음 할 일
- [ ] Phase 1 본격화: 수집 기간 6개월~1년으로 확장, 종목당 뉴스량 확대
- [ ] 뉴스 노이즈 필터링 (경제/증권 섹션 위주, 키워드 동시 등장 조건)
- [ ] 뉴스-주가 매칭 로직 설계 (발행일 → 다음 거래일 종가 변동률)
- [ ] Phase 2: 라벨링 규칙(상승/하락/중립 임계값) 확정, EDA

### 산출물
- `scripts/collect_sample.py`, `scripts/collect_multi_sample.py`, `scripts/test_naver_news_api.py`, `scripts/test_qwen_load.py`
- `.env` / `.env.example` (API 키 관리)
- `data/raw/*.csv` (테스트 수집 결과, git 미포함)
