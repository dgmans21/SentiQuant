# 작업 일지

새 작업일마다 최상단에 날짜 섹션을 추가한다 (최신순).

---

## 2026-09-02 (9/1~9/2에 걸친 작업)

### 뉴스 수집 계속 (9/1, 9/2)
- 매일 루틴대로 큐레이션(삼성전자·하이닉스) + 전종목 백필 실행
- 백필 누적: 41,903건(9/1) → 46,970건(9/2)

### yfinance 코스피 지수 8/28 결측 — 진짜 원인 찾음
- 9/1 새벽 최초 발견: 8/31 종가 전종목 NaN + 코스피 지수만 8/28이 통째로 빠짐 → 처음엔 "일시적 지연"으로 판단, 다음날 재시도하기로 함
- 9/2 재확인: 8/31은 정상 채워졌으나 **8/28 코스피 결측은 그대로 재현됨** — 재시도 3회(기본 재요청, 다음날 재요청, `yf.download`에 명시적 start/end 지정)해도 동일 → 일시적 문제가 아니라 Yahoo 소스 자체에서 그 날짜의 `^KS11` 데이터가 빠진 것으로 확정 (개별 종목엔 8/28 데이터가 정상적으로 존재함, 지수만 없음)
- **해결**: `collect_index_history.py`를 수정해, 개별 종목 가격(`price_history.csv`)의 실제 거래일 캘린더를 기준으로 지수 시계열을 reindex한 뒤 내부 결측만 선형보간(`interpolate(inside)`)하도록 함 — 최신일(아직 미발표)은 보간 대상에서 자동 제외됨(양쪽 끝점이 있어야 보간되므로). 앞으로 같은 종류의 고립된 단일일 결측이 생기면 자동으로 복구됨
- 이 버그 때문에 `add_excess_return.py`에서 결측치가 5,107건(8/27→8/28 매칭분) → 12,573건(8/28→8/31 매칭분 추가)까지 쌓였다가, 수정 후 0건으로 해결됨

### GPU 자원 경합으로 실제 드라이버 크래시 발생 — 원인 규명
- 라벨링 도중 속도가 평소(1.3~1.5건/초)보다 크게 느려짐(0.89건/초) + 사용자가 nvidia 오류 메시지 목격
- `nvidia-smi`로 확인해보니 **별도 conda 환경(`tts_v2`)의 GPT-SoVITS 웹UI 서버가 VRAM ~3GB를 상시 점유한 채 백그라운드 실행 중**이었음
- Windows 이벤트 로그(`nvlddmkm`, Event ID 13/153)에서 라벨링 중이던 시각과 정확히 겹치는 **실제 GPU 드라이버 타임아웃/강제복구(TDR) 기록** 확인 — 단순 느려짐이 아니라 진짜 드라이버 크래시였음
- 원인: TTS 모델이 로드된 채로 떠있기만 해도(추론 안 하고 있어도) VRAM은 계속 점유됨 → Qwen이 필요로 하는 VRAM과 합쳐서 8GB 한도 근처에서 충돌
- 사용자가 TTS 서버 종료 후 100건 스피드테스트(1.1~1.2건/초, 정상 범위) → 이후 청크들 1.4~1.56건/초로 완전 정상화 확인
- **교훈**: 다른 GPU 프로세스는 "지금 뭘 시키고 있냐"가 아니라 "모델이 로드되어 켜져 있냐"가 VRAM 점유 여부를 결정함 — 다음에 라벨링/학습 전에 `nvidia-smi`로 다른 프로세스 확인하는 걸 습관화할 것

### Qwen 라벨링 백로그 전량 처리 (7,466건)
- 800~1000건씩 9번에 나눠 실행, 전량 완료(36,792/36,792, 백로그 0)
- split 재생성: 30,511건(무관/파싱실패 제외, 이전 24,695건 대비 +5,816) — train 21,357 / val 4,577 / test 4,577

### 재학습 — v5, 정체 재확인
- `models/klue-bert-qwen-sentiment-v5`: **test accuracy 83.8%, F1(macro) 82.9%** — v4(83.5%/82.2%) 대비 거의 그대로
- 데이터 24,695→30,511건(+24%)에도 성능 정체 재확인 — 이 아키텍처의 한계라는 기존 결론이 또 한 번 검증됨. **v5가 현재 최종 모델**

### 디스크/백업 정리
- `backup/` 폴더 최신화(덮어쓰기): 세 파일 다 이어붙이기 방식이라 신버전이 구버전을 항상 포함 — 버전 보존할 실익 없다고 판단, 덮어쓰는 쪽으로 정리 (37MB → 47MB)
- `models/`: v4 삭제(v5로 대체됨) + v5의 `checkpoint-*` 서브폴더 삭제 → 다시 423MB로 정리
- `data/processed/news_labeled*.csv` 계열(구 주가기반 라벨 2개 + Qwen 라벨링용 임시 스냅샷 v2~v6, 총 7개 ~58MB) 전부 삭제 — 전부 `news_price_matched.csv`에서 그때그때 재생성 가능한 임시 파일들

### 남은 것 (다음에 할 일)
- [x] RAG 데모 임베딩(`news_embeddings.npy`) 30,511건으로 갱신 완료
- [ ] (기존 목록 계속) RAG 검색 품질 체계적 평가, 스크린샷 그리드 레이아웃, 배포 여부 결정

---

## 2026-08-31

### README를 외부(이력서 방문자)용으로 재작성, 내부 기록은 WORKLOG로 이관
기존 README.md는 "다음 세션이 이어갈 수 있도록"이라는 내부 인수인계 문서 톤이었음 — 이력서 보러 오는 사람 기준으로는 안 맞다고 판단, 완전히 새로 씀(핵심 결과 요약 + "이 프로젝트가 보여주는 것" + 시스템 구성 + 로컬 실행법). 아래는 기존 README에만 있던 내용 중 보존할 것들.

**모델 비교표 (최종: `models/klue-bert-qwen-sentiment-v4`)**

| 모델 | Test Accuracy | F1(macro) | 상태 |
|---|---|---|---|
| 주가 기반 라벨 (최고 기록) | 38.5% | 38.5% | 폐기, 참고용 |
| 논조 라벨 v1 (전환 직후, split 버그 있었음) | 81.3% | 79.3% | 폐기 |
| 논조 라벨 v3 (split 버그 수정: 날짜 기준 → (종목×라벨) stratified) | 83.0% | 82.0% | 폐기 |
| **논조 라벨 v4 (최종)** | **83.5%** | **82.2%** | **현재 최종 모델** |

v1→v3 사이 버그: 날짜 기준 split이 18종목 중 10종목을 val/test에서 완전히 누락시키고 있었음(81.3%가 사실상 상위 5종목 평균이었음) — (종목×라벨) 행 단위 stratified 방식으로 교체. 경위는 2026-08-28 항목 참고.

**스크립트 맵**

| 스크립트 | 역할 |
|---|---|
| `collect_multi_sample.py` | 20종목 리스트(`STOCKS` dict) 정의 + 소량 테스트 수집 |
| `collect_news_backfill.py` | 종목당 최대 1000건 뉴스 백필 (재실행 시 기존과 merge+dedup) |
| `collect_news_curated.py` | 삼성전자·SK하이닉스 전용 일일 소량 큐레이션 수집 |
| `collect_price_history.py` / `collect_index_history.py` | 종목/코스피 지수 1년치 주가 (매칭 전 항상 먼저 갱신) |
| `match_news_price.py` | 뉴스+주가 매칭 (backfill + curated 자동 병합) |
| `add_excess_return.py` | 코스피 대비 초과수익률 계산, `news_price_matched.csv`에 컬럼 추가 |
| `label_qwen_full.py` | Qwen3 논조 라벨링 (배치+체크포인트, 목표건수 인자로 짧게 끊어서 실행 가능) |
| `split_qwen_labeled.py` | Qwen 라벨에서 "무관" 제외 + (종목×라벨) stratified train/val/test 분할 |
| `train_classifier.py` | KLUE-BERT 파인튜닝 (데이터/출력 경로 인자로 받음) |
| `label_and_split.py`, `find_good_seed.py` | 주가 기반 라벨링 시절 스크립트 (현재 미사용, 참고용 보존) |
| `check_*.py` | QA/진단 유틸리티 (분포 확인, 갭 확인 등) |
| `pilot_qwen_labeling.py` | Qwen3 라벨링 소규모 파일럿 테스트용 |
| `build_news_embeddings.py` | 뉴스 전량을 `jhgan/ko-sroberta-multitask`로 임베딩 (1회성) |
| `rag_pipeline.py` | `SentimentRAG` 클래스 — 분류+검색+Qwen 설명을 하나로 묶음 |
| `api/main.py` | FastAPI 서버 (`/analyze`, `/explain`) |
| `web/` | Next.js UI |

**다음에 할 일 (우선순위 순, 2026-08-30 기준으로 확정된 목록)**
1. RAG 검색 품질 체계적 평가 — 지금은 수작업 스팟체크 수준. 쿼리 기사 50~100건 평가셋 구성 → Precision@5 등 지표화 → BM25 키워드 검색 대비 비교
2. RAG 데모 스크린샷 그리드(`portfolio/sentiquant.html` 06번 섹션)를 3열 → 1열로 레이아웃 변경 (방향만 정함, 미적용)
3. 네이버 뉴스 링크 스크래퍼 추가(선택), UI 스트리밍/병렬화, 배포 여부 결정
4. (참고, 착수 안 함) 뉴스 외 주식 커뮤니티 텍스트까지 학습 확장 — 공식 API 없어 스크래핑 회귀 필요, 노이즈 심함. 대략 3~5일 예상
5. "논조-수익률 효과크기가 작다"는 결론 자체는 개선 대상 아님(세 번의 분석에서 r=0.04~0.13으로 수렴 확인, 억지로 키우면 p-hacking) — 종목/섹터별 세분화나 논조 강도 기반 재분석 등 더 정교하게 파고드는 방향은 여지 있음

### 디스크 정리 — models/ 33GB → 423MB
- 원인: `Trainer` 자동 체크포인트(`checkpoint-N`)가 옵티마이저 상태까지 통째로 저장 — 모델당 체크포인트 3개 × 1.3GB, 실제 필요한 최종 파일(safetensors+config)은 424MB뿐이었음
- v4 제외 구모델 전체(`klue-bert-return`, `-excess`, `-excess-v2`, `-sentiment`, `-qwen-sentiment` v1~v3) 삭제 — 수치는 위 비교표에 이미 보존됨
- v4의 `checkpoint-*` 서브폴더도 삭제, 실제 사용 파일만 남김

### 백업 폴더 구성 — "복구 불가능한 것만" 기준
`backup/` 폴더에 아래 3개만 복사(총 37MB). 기준: NAVER API가 최신 데이터만 보여줘서 지난 뉴스는 재수집 불가능하거나, 재생성에 GPU 시간이 크게 드는 것만 선정. 가격 데이터(yfinance로 언제든 재수집 가능), 파생 CSV(스크립트로 몇 분이면 재생성), 모델 가중치(재학습 7분)는 제외.
- `data/raw/news_backfill.csv` (17MB)
- `data/raw/news_daily_curated.csv` (844KB)
- `data/processed/news_qwen_labeled.csv` (20MB, Qwen 20시간+ GPU 라벨링 결과물)

### .gitignore 정리
`web/node_modules/`, `web/.next/`(Next.js 생성물), `Entropy645_포트폴리오.html`(다른 프로젝트 파일, 실수로 섞여 있었음) 추가. 이후 사용자가 직접 `img/`, `portfolio/`도 추가함(개인 사진·이메일이 담긴 포트폴리오 HTML과 스크린샷을 공개 저장소에는 안 올리기로 결정한 것으로 보임).

## 2026-08-30

### 포트폴리오 페이지(`portfolio/sentiquant.html`) 버그 수정
- "주가 기반 라벨 — 실험 3회" 막대그래프가 안 보이는 문제 → `<rect>`의 `height`에 음수값(`-56`/`-70`/`-77`)이 들어가 있어서 SVG가 렌더링을 거부한 게 원인. 양수 height + 정확한 y좌표로 수정
- 소제목(`<h2>`)들이 전부 의도한 2줄이 아니라 3줄로 꺾이는 문제 → 공통 CSS `.section-head h2{ max-width:20ch }`가 원인. `ch`는 한글엔 너무 좁은 기준(한글 1자가 라틴 문자보다 훨씬 넓음)이라 `<br>`로 나눈 각 줄이 또 한 번 자동 줄바꿈됐던 것. `max-width` 제거로 전체 일괄 해결
- 05→06 섹션 전환이 끊겨 보이는 문제(왜 RAG로 방향을 틀었는지 설명 없이 바로 넘어감) → "이 정도 신호로 확정적 예측을 내세우는 건 정직하지 않다고 판단했다..." 전환 문장 추가
- RAG 데모 스크린샷 3장이 3열 그리드 + `object-fit:cover` 고정높이(260px)에 눌려 과도하게 잘림(`기본화면.jpg`는 가로 60% 가까이 손실) → 1차로 클릭 시 원본 비율 전체화면 라이트박스(이전/다음/ESC 지원) 추가. 다만 "포트폴리오는 클릭 안 해도 스크롤만으로 파악돼야 한다"는 논의 끝에 **기본 썸네일 자체를 1열로 바꿔 안 잘리게 하는 게 맞다는 결론 — 아직 미적용, 다음에 마저 할 것**

### 논의만 하고 실행 안 한 것 (기록용)
- 뉴스 외 **주식 커뮤니티 텍스트**(종목토론실 등)까지 학습 확장 아이디어 → 공식 API 없어 스크래핑 회귀 필요, 노이즈 훨씬 심함, 약관 회색지대 등으로 뉴스보다 확실히 어려움. 대략 3~5일 예상(스크래퍼 작성 1일 + 수집 1일 + Qwen 프롬프트 재검증 0.5~1일 + 라벨링·재학습 반나절~1일). **착수 안 함, 아이디어만 기록**
- "한계" 섹션 두 항목 검토:
  - RAG 검색 품질(수작업 스팟체크 수준) → **개선 가능**, 다음 과제로 채택 (아래)
  - 논조-수익률 효과크기(r²<2%) → 이미 세 번의 분석에서 같은 좁은 범위(r=0.04~0.13)로 수렴 확인된 정직한 결론. 숫자를 키우려는 시도는 p-hacking에 해당해서 **"개선 대상 아님"으로 확정** — 다만 종목/섹터별 세분화나 논조 강도 기반 재분석 등 "더 정교하게 파고드는" 방향은 여지 있음

### 다음 할 일
- [ ] **RAG 검색 품질 체계적 평가** (다음 과제로 확정): 쿼리 기사 50~100건 평가셋 구성 → Precision@5 등 지표화 → BM25 키워드 검색 대비 비교
- [ ] RAG 데모 스크린샷 그리드 1열로 레이아웃 변경 (위에서 방향만 정하고 미적용)
- [ ] (이전 목록 계속) 네이버 뉴스 링크 스크래퍼 추가(선택), UI 스트리밍/병렬화, 배포 여부 결정

---

## 2026-08-29

### 논조-수익률 역상관 원인 분석 — 다중 시차(t+1/3/5/10) 검증
- t+1에서만 보고 "역상관"이라 결론짓기엔 성급하다는 문제의식 → 시차를 늘려가며 재검증
- 동일 코호트(t+10까지 데이터 있는 2,949건, 표본 구성 편향 제거 위해 고정) 기준:
  - t+1: r=-0.066 (역상관 유지)
  - t+3: r=+0.080 (부호 반전)
  - t+5: r=+0.089
  - t+10: r=+0.078 (양의 방향 유지)
- **결론**: 다음날(t+1)만 보면 과잉반응/역인과로 역상관처럼 보이지만, 3거래일 이후부터는 논조가 기대 방향(긍정↔상대적 호실적)대로 자리잡아 t+10까지 유지됨. 단기 과잉반응 후 되돌림 + 중기 드리프트 패턴과 부합. r 자체는 여전히 작아(설명력 <1%) "매매전략"보다는 "논조에 정보가 있으나 반영에 며칠 걸린다"는 통계적 증거 수준
- **한계**: 이 고정 코호트가 저빈도 종목(POSCO홀딩스·신한지주·LG화학 등) 위주로 편향됨 — 고빈도 종목(삼성전자·카카오 등)까지 일반화하려면 시간이 더 지나서 걔들도 t+10 데이터가 쌓여야 확인 가능
- 산출물: `data/processed/news_multi_horizon.csv`

### RAG 데모 구현 — 논조 분류 + 유사사례 검색 + Qwen 설명
- 프로젝트 방향 논의: "논조-주가 상관관계"는 효과크기가 작아 데이터를 더 모아도 안 커진다는 결론(오늘 앞선 분석들의 일관된 r 범위 0.04~0.13 근거) → "모델 성능 극대화"보다 "정직한 파이프라인 완성 + RAG로 확장"에 집중하는 방향으로 정리
- 재활용성 점검: klue-bert-v4를 일반 커뮤니티 텍스트(반어법·은어)에 적용 → 실패(전부 "중립"으로 회피, 확신도 낮음). 반면 애널리스트 리포트·전자공시 스타일에는 잘 작동(형용사 없는 건조한 공시문도 정확히 판단) — "격식체냐"가 아니라 "학습 도메인(금융뉴스)과 같은 종류냐"가 기준이었음이 확인됨
- **RAG 파이프라인 구축**:
  1. `scripts/build_news_embeddings.py`: `jhgan/ko-sroberta-multitask`로 뉴스 24,695건 임베딩 (50초 소요)
  2. `scripts/rag_pipeline.py`: `SentimentRAG` 클래스 — klue-bert 즉시분류 + 코사인 유사도 검색(numpy) + 유사 과거사례의 실제 t+1~t+10 초과수익률 조회 + (지연로딩) Qwen3-4B 설명 생성
  3. `api/main.py`: FastAPI, `/analyze`(즉시) · `/explain`(Qwen, 첫 호출만 느림 ~32초, 이후 ~12초) 분리 — Qwen을 항상 띄워두지 않고 "추가 분석" 버튼 눌렀을 때만 지연 로딩하는 설계로 VRAM 절약(klue-bert+임베딩+Qwen 동시 로드 시 5.4GB/8GB, 여유 있음)
  4. `web/`: Next.js 16 최소 UI — 텍스트 입력 → 즉시 분류+유사사례 표 → "추가 분석" 버튼으로 Qwen 설명
- 설계 원칙: 링크가 아닌 텍스트 입력만 지원(스크래퍼 미구현, 의도적 범위 제한), Qwen 응답에도 "상관관계 약함/참고용" 문구를 프롬프트로 강제해서 과대해석 방지
- 로컬에서 end-to-end 테스트 완료 (`http://localhost:3000` ↔ FastAPI `:8000`)

### 다음 할 일 (RAG 데모)
- [ ] 네이버 뉴스 링크 스크래퍼 추가 (선택)
- [ ] UI 다듬기, "즉시결과 먼저 → Qwen 설명 나중" 스트리밍/병렬화
- [ ] 배포 여부 결정, README에 전체 서사 정리

### 역인과관계 검증 — 약한 신호 확인 (과대해석 주의)
- 가설: "부정" 라벨이 미래 하락을 예측하는 게 아니라, 이미 하락한 주가를 설명하는 기사에 붙는 꼬리표일 수 있다
- 발행일 기준 이전 t-1/t-3/t-5 거래일 초과수익률 vs 논조 라벨 상관관계 검증 (표본 24,695건):
  - t-1: r=+0.123 (지금까지 본 상관계수 중 제일 큼), 부정=-1.50% / 중립=-0.36% / 긍정=+0.16%
  - t-3: r=+0.039, t-5: r=+0.060 — 전부 방향 일치(부정<중립<긍정)
- **결론(수정)**: r=0.123은 Cohen 기준으로 "작은 효과" 구간 초입(r²≈1.5%)일 뿐, "강하게 확인됨"이라 표현한 건 과장이었음. "역인과관계일 가능성을 시사하는 약한 통계적 신호" 정도가 정확한 표현. "이미 하락한 뒤에야 기사가 나온다"는 단정도 무리 — 일별 종가 데이터만으로는 (1)기사가 가격을 뒤쫓는 경우와 (2)제3의 사건이 가격·기사 둘 다에 며칠에 걸쳐 영향을 주는 경우를 구분할 수 없음
- t+3~t+10 드리프트에 사전 모멘텀이 얼마나 섞였는지는 여전히 복합요인 분석(사전 모멘텀 통제) 없이는 확정 불가 — 효과크기가 작아서 우선순위 낮게 보류 중
- 산출물: `data/processed/news_pre_news_return.csv`

### v1 최종 마무리
- 20종목 전체 재백필(4,184건 회수) → 가격/지수 갱신(8/28 종가까지) → 매칭 29,326건 → Qwen 라벨링 백로그(8,074건) 전량 처리 → split 재생성(24,695건, train 17,286/val 3,704/test 3,705)
- `label_qwen_full.py` BATCH_SIZE 16 실험 → 오히려 느려짐(1.1건/초 vs 배치8의 1.3~1.5건/초, 프롬프트 길이 편차로 인한 패딩 낭비 추정) → 8로 원복
- 최종 재학습: `models/klue-bert-qwen-sentiment-v4`, **test accuracy 83.5%, F1(macro) 82.2%** — v3(83.0%/82.0%) 대비 거의 그대로, 이 아키텍처(klue-bert-base 단일분류)의 성능 한계에 도달한 것으로 결론. **v1 최종 모델로 확정.**
- 어제 논조-수익률 역상관 발견(긍정 -1.13% vs 부정 +0.86%)은 다음 세션에서 원인 분석 이어갈 것

## 2026-08-28

### 삼성전자·SK하이닉스 파이프라인 편입 + split 방법론 버그 수정
- 삼성전자·SK하이닉스 큐레이션 데이터 매칭(2,852건) → 전체 라벨링 파이프라인에 합류
- **종목별 test 표본 점검 중 큰 버그 발견**: 기존 `split_qwen_labeled.py`가 "날짜" 단위로 70/15/15 분할하는 방식이라, 18종목 중 10종목(카카오·KB금융·KT·LG에너지솔루션·NAVER·SK텔레콤·기아·삼성물산·한화에어로스페이스·현대차)이 val/test에 단 한 건도 없이 전부 train에만 존재했음. 81.3% 정확도는 사실상 8종목(그중 5종목이 96.6%)만으로 측정된 값이었음.
- **원인**: 날짜 기준 split은 주가 기반 라벨 시절 "동일 날짜 시장 전체 움직임 leakage 방지"용 설계였는데, Qwen 논조 라벨은 텍스트 자체 속성이라 그 우려가 무의미함. → **(종목 x 라벨) 기준 행 단위 stratified split으로 교체**
- 가격/지수 갱신 + 매칭 재실행 → 전체 20종목 21,252건 매칭, 이 중 7,212건이 "가격매칭은 됐지만 Qwen 라벨링은 안 됨" 상태로 밀려있던 것 추가 발견 (v2/v3 스냅샷이 예전 시점에 얼어있었던 게 원인) → 전부 라벨링 완료(21,252/21,252)
- 최종 재학습(`models/klue-bert-qwen-sentiment-v3`, 18,196건): **test accuracy 83.0%, F1(macro) 82.0%**
- 종목별 정확도 스프레드가 이전(73.3~92.0%, 18.7%p)에서 지금(78.6~87.9%, 9.3%p)으로 절반 가까이 줄어듦 — 이전에 "약점"으로 보였던 신한지주(75.4%)가 사실 표본 부족발 노이즈였고, 지금은 80.0%로 평균 근처임이 드러남

### 종목별 뉴스 수집 빈도 분석
- 백필이 API 1000건 한도에 걸려서, 하루 발행량 많은 종목일수록 커버 기간이 짧아짐을 발견 (삼성전자 663건/일→3일 커버, POSCO 7.7건/일→120일 커버)
- 카카오(345/일)·현대차(245/일)·KT(242/일)·기아(182/일)·SK텔레콤(147/일)·삼성물산(96/일)이 하이닉스급 고빈도였는데 일일 수집 루틴이 없어서 놓칠 뻔함
- 전체 재백필 실행 → 6,354건 신규 회수 (카카오 680, 기아 369 등). "며칠 안에 다시 안 돌렸으면 API에서 영구 유실될 뻔한" 분량이었음
- `DATA_PIPELINE_RULES.md` 신설: 파이프라인 순서, 종목별 수집주기 티어, 재학습 전 체크리스트, 파일 버전 메모 정리

### 뉴스 논조 vs 실제 주가 상관관계 1차 분석 (핵심 가설 검증)
- 표본 18,196건, 논조별 초과수익률(KOSPI 대비, 다음 거래일): 부정 +0.86% > 중립 -0.08% > **긍정 -1.13%**
- 논조(순서형 -1/0/1) vs 초과수익률 피어슨 상관계수 **r=-0.169, p<0.0001** — "긍정 뉴스일수록 다음날 초과수익률이 낮다"는, 순진한 가설과 반대 방향의 통계적으로 매우 유의한 신호
- **해석은 아직 미확정**: (1) 이미 주가에 선반영된 뒤 차익실현, (2) 역인과관계(주가 하락을 설명하는 기사가 부정으로 라벨되고 다음날 반등), (3) 매칭 로직상 단기 과잉반응 되돌림을 포착했을 가능성 — 다음 세션에서 원인 구분 분석 필요

### 다음 할 일
- [ ] 논조-수익률 역상관 원인 규명 (섹터별/시차별 분석, 당일 vs 익일 수익률 비교 등)
- [ ] 내일: 전체 20종목 백필 재실행 → 가격/지수 갱신 → 매칭 → 라벨링 백로그 정리 (매칭 vs 라벨링 갯수 갭 체크 필수, `DATA_PIPELINE_RULES.md` 참고)
- [ ] 데이터 ~22,000~25,000건 정도 쌓이면 재학습 1회 더 (v1 최종 후보)
- [ ] 이후 방향: 데이터 양보다 "논조-주가 상관관계 분석 심화" 및 README/스토리텔링(로드맵 Phase 9)에 집중 권장

### 산출물
- `DATA_PIPELINE_RULES.md` (신규)
- `models/klue-bert-qwen-sentiment-v2`, `models/klue-bert-qwen-sentiment-v3` (현재 최종)
- `data/processed/news_sentiment_final.csv` (18,196건, 종목 stratified split)
- `scripts/split_qwen_labeled.py` (날짜 기준 → 종목x라벨 기준으로 교체)

---

## 2026-08-27

### Qwen3-4B 논조 라벨링 완료
- 남은 3,996건을 `label_qwen_full.py`로 목표건수 단위(1000/1200/596/596/296/300건)로 끊어서 이어서 실행, **전체 11,188건 라벨링 완료**
- 최종 분포: 중립 5,017건(44.8%) / 긍정 3,172건(28.4%) / 부정 1,661건(14.8%) / **무관 1,333건(11.9%)** / 파싱 실패 5건(0.04%)
- 결과 파일: `data/processed/news_qwen_labeled.csv` (11,188행)

### 논조 라벨로 KLUE-BERT 재학습 — 대성공
- "무관"/파싱실패 제외 → 9,850건, `split_qwen_labeled.py`로 날짜 그룹+랜덤셔플 split (train 8,589/val 522/test 739, 최적 시드 75)
- `train_classifier.py`의 `LABEL2ID`를 부정/중립/긍정으로 교체 후 재학습 → **test accuracy 81.3%, F1(macro) 79.3%**
- 혼동행렬: 부정 회수율 73.3% / 중립 80.8% / 긍정 86.7% — 세 클래스 모두 균형 있게 구분, 한쪽으로 쏠리는 패턴 완전히 사라짐
- **비교**: 주가 기반 최고 기록(38.5%) 대비 두 배 이상. "과제 재정의"가 가설대로 완전히 유효했음이 실증됨
- 결과: `data/processed/news_sentiment_final.csv`, `models/klue-bert-qwen-sentiment`
- 삼성전자·SK하이닉스 일일 큐레이션 누적 404건 (앞으로 3~5일치면 다음 재학습에 포함시키기 충분 — 논조 라벨은 날짜 다양성이 굳이 필요 없어서 예전보다 훨씬 빨리 쓸 수 있게 됨)

### 다음 할 일
- [ ] "논조 예측 vs 실제 주가" 사후 상관관계 분석 설계 (긍정 판정 기사들이 실제로 주가와 상관있는지)
- [ ] (병행) 삼성전자·SK하이닉스 일일 큐레이션 계속, 3~5일 쌓이면 라벨링 후 재학습에 포함
- [ ] Phase 4(Qwen3-4B LoRA 파인튜닝) 또는 Phase 5+(배포/대시보드)로 진행 여부 논의

---

## 2026-08-26

### Phase 3 — KLUE-BERT 파인튜닝 1차 시도 (주가 기반 라벨)
- 8,140건(원래 수익률, ±1.0% 임계값)으로 학습 → **test accuracy 25.7~31.7%, F1(macro) 25.8~30.3%** (찍기 확률 33.3%보다 낮거나 비슷)
- 혼동행렬 확인 결과 모델이 특정 클래스(상승 또는 중립)로 쏠려 도망가는 패턴, 실질적 신호 학습 실패
- **원인 진단**: (1) 헤드라인 텍스트만으로 익일 주가 방향을 예측하는 과제 자체가 원래 어려움(효율적 시장 가설과 관련), (2) 시장 전체가 오르내리는 날엔 무관한 기사도 다 같은 라벨이 붙는 라벨 노이즈

### 개선 시도 1 — 시장 대비 초과수익률 라벨링
- 코스피 지수(^KS11) 수익률을 종목 수익률에서 빼서 "초과수익률" 계산 (`scripts/add_excess_return.py`)
- 재학습 결과: accuracy 35.0%, F1 32.2% — 찍기 확률 첫 돌파, 다만 혼동행렬 보면 "중립"으로 쏠리는 패턴 여전
- **Split 시행착오 추가 발견**: 날짜가 111개뿐이라 무작위 셔플이라도 시드에 따라 라벨 분포가 크게 출렁임 → 여러 시드 탐색 후 최적 시드 채택 (`scripts/find_good_seed.py`)

### 개선 시도 2 — 데이터 확장
- 18개 종목 백필 재실행(15,877→21,286건), 주가/지수 데이터 갱신, 보류됐던 매칭 재시도, 삼성전자·SK하이닉스 큐레이션 데이터(112건) 매칭 파이프라인에 연결
- 매칭 결과 8,140→11,188건으로 확장
- 재학습 결과: **accuracy 38.5%, F1(macro) 38.5%** — 지금까지 최고 성능. 혼동행렬도 한 클래스로 쏠리지 않고 세 방향 다 어느 정도 구분 (하락 회수율만 약함, 24%)

### 방향 전환 논의 — "뉴스만으로 주가 예측"의 한계 인정
- 사용자가 "기사만으로 주가 예측은 과도한 기대 아니냐"는 의문 제기 → 타당한 지적으로 판단
- 대안: 라벨을 "주가 변동"이 아니라 "기사 자체의 논조(긍정/부정/중립)"로 바꾸고, 주가는 학습 후 별도 검증 단계로 이동
- 논조 라벨은 Qwen3-4B로 자동 생성하기로 결정 (사람이 대량 수작업 라벨링 어려움)

### Qwen3-4B 자동 라벨링 파일럿
- 1차 테스트: `max_new_tokens=80`이 너무 짧아 Qwen3의 "생각 모드"(`<think>`)에 응답이 전부 잘림 → `enable_thinking=False`로 해결
- 2차 테스트(25건, 3분류): 대체로 합리적이나 **무관 기사(예: "KT" 검색에 kt wiz 야구팀 기사가 걸림)를 못 거름**
- 3차 테스트(25건, 4분류로 "무관" 옵션 추가): 무관 기사 5/5 정확히 걸러냄, 노이즈 필터링 문제 해결 확인
- 속도: 건당 3.7초(비배치) → 배치 처리(`BATCH_SIZE=8`)로 초당 1.3~1.5건까지 개선 (약 5배)
- 전체 11,188건 예상 시간 약 2시간 → 10분/지정 건수 단위로 끊어서 체크포인트 저장하며 진행하는 방식 채택 (`scripts/label_qwen_full.py`, 링크 기준 재시작 가능)
- 오늘 최종 4,000/11,188건(35.8%) 완료 (10분 타임박스 1회 + 목표건수 지정 3회, 총 4번 실행)
  - 라벨 분포(3,000건 시점): 중립 41.2% / **무관 22.7%** / 긍정 19.3% / 부정 16.8%, 파싱 실패 0건
  - **무관 22.7%라는 수치로, 기존에 우려했던 키워드 검색 노이즈 문제가 실제로 상당했음을 정량적으로 확인**
- 실행 시간 실측(스크립트 자체 기준, 모델 로딩 포함): 880건/10.2분, 1,120건/14.6분, 1,000건/12.4분, 1,000건/약 12~13분 — 평균 초당 1.3건 내외로 안정적
- 사용자가 컴퓨터를 켜놓고 자고 싶지 않다고 해서, 매번 목표 건수를 지정해 짧게 끊어 실행하고 다음 진행 여부를 묻는 방식으로 운영 중 (체크포인트가 링크 기준이라 언제 끊어도 안전)
- GPU 발열 확인(사용자 우려로 실측): 유휴 43~45도, 라벨링 작업 중 61~63도에서 안정 (4060 Ti 안전 한계 88~90도 대비 여유 있음, 전력 105~130W/160W). 문제 없음으로 확인

### 1차 결과 (2026-08-26 세션 종료 시점, 7,192/11,188건, 64.3%)
| 라벨 | 건수 | 비율 |
|---|---|---|
| 중립 | 3,312 | 46.1% |
| 긍정 | 1,800 | 25.0% |
| 부정 | 1,045 | 14.5% |
| 무관 | 1,030 | 14.3% |
| 파싱 실패 | 5 | 0.07% |

3,000건 시점(무관 22.7%)보다 후반부 무관 비율이 낮아짐(14.3%) — 데이터 순서상 뒤쪽 구간이 상대적으로 노이즈가 적었던 것으로 추정. 전체 완료 후 최종 분포는 달라질 수 있음.

### 다음 할 일
- [ ] Qwen 라벨링 이어서 진행 (남은 3,996건) — `python scripts\label_qwen_full.py <목표건수>`
- [ ] 전체 완료 후: "무관" 제외하고 긍정/부정/중립 3분류로 KLUE-BERT 재학습, 성능 비교
- [ ] 재학습 후 "주가 검증 단계" 설계: Qwen이 긍정 판정한 기사들이 실제로 주가와 상관있는지 사후 분석
- [ ] (병행) 삼성전자·SK하이닉스 일일 큐레이션 계속, 보류 매칭 주기적 재시도

### 산출물
- `scripts/add_excess_return.py`, `scripts/find_good_seed.py`, `scripts/train_classifier.py`(경로 인자화)
- `scripts/pilot_qwen_labeling.py`, `scripts/label_qwen_full.py`, `scripts/check_qwen_progress.py`
- `data/processed/news_labeled_excess_v2.csv` (11,188건, 주가 기반 라벨)
- `data/processed/news_qwen_labeled.csv` (진행 중, 3,000/11,188건 논조 라벨)
- `models/klue-bert-return`, `models/klue-bert-excess`, `models/klue-bert-excess-v2` (실험별 체크포인트, git 미포함)

## 2026-08-25

### Phase 1 마무리 — 백필 + 뉴스-주가 매칭
- 20종목 각각 최대 1000건(API 한도) 백필 시도 → 첫 실행에서 종목별 편차 발견 (453~998건). 재조사 결과 진짜 원인은 **페이지네이션 드리프트**: 여러 페이지를 순차 호출하는 동안 실시간으로 새 기사가 계속 올라와서, 날짜순 정렬 결과가 호출 사이에 밀리며 중복/누락이 생김. 버그도 콘텐츠 한계도 아니라 자연스러운 현상으로 결론, 재시도 로직 추가 후 최종 15,877건(dedup) 확보
- 주가 데이터를 1개월 → **1년치로 재수집** (뉴스가 4월 말까지 있어서 커버 필요)
- 뉴스-주가 매칭 로직 구현: 뉴스 발행일 → **다음 거래일** 종가 변동률(전일 대비 %) 계산 (`scripts/match_news_price.py`)
  - 8,140건 매칭 성공, 7,737건은 최근(08-21~08-25) 뉴스라 다음 거래일 주가가 아직 데이터 소스에 안 올라와서(NaN) 매칭 보류 — 원본은 보존되며 며칠 뒤 재실행하면 자동 회수됨
  - 검증: 수동 계산(LG에너지솔루션 08-20→08-21 -4.05%)과 대조해서 로직 정확성 확인
- 삼성전자·SK하이닉스처럼 뉴스량이 너무 많아 1000건이 반나절 만에 소진되는 종목 대응: 하루 소량(80건)씩 제목 유사도 기반 중복 제거 + 언론사 쏠림 방지하며 큐레이션하는 `scripts/collect_news_curated.py` 제작 (매일 수동 실행 예정, 사용자 담당)

### Phase 2 — 라벨링 + Split
- 임계값 실험: 데이터 실제 분포(평균 +0.28%, 표준편차 3.29%) 기준으로 여러 후보 비교 → **±1.0%**가 우연히도 하락/중립/상승을 32.6/33.6/33.8%로 거의 완벽하게 3등분함, 이걸로 채택
- Train/val/test 분할에서 시행착오:
  1. 날짜 개수 기준 70/15/15 → 최근 날짜에 뉴스량이 쏠려서 실제 건수는 test 83% vs train 9%로 뒤집힘
  2. 건수 기준 블록 분할로 수정 → 블록 생성 로직 버그로 크기 왜곡
  3. 날짜 단위 7일 주기 라운드로빈으로 단순화 → **요일 효과**로 추정되는 라벨 쏠림 재발견 (test 하락 72%, val 상승 63%)
  4. 최종: 날짜를 고정 시드로 **랜덤 셔플 후 70/15/15 분할** (그룹 단위는 날짜 유지 → 같은 날 중복 기사가 train/test에 동시에 들어가는 누출은 방지) → train 5,457 / val 2,008 / test 675, 세 split 모두 합리적으로 균형
- 결과: `data/processed/news_labeled.csv` (label, split 컬럼 포함)

### 다음 할 일
- [ ] EDA (라벨 분포, 종목별/섹터별 시각화) — 선택 사항, Phase 3 진행에 필수는 아님
- [ ] 며칠 후: `collect_price_history.py` → `match_news_price.py` 재실행해서 보류된 7,737건 회수
- [ ] 삼성전자·SK하이닉스: `collect_news_curated.py` 매일 실행해서 누적
- [ ] Phase 3: KLUE-BERT/FinBERT-kr 파인튜닝 (예상: GPU 학습 자체 10~30분, 전체 작업 1~3시간)

### 산출물
- `scripts/collect_price_history.py`, `scripts/match_news_price.py`, `scripts/collect_news_curated.py`, `scripts/label_and_split.py`
- `scripts/check_backfill_range.py`, `scripts/check_match_gap.py`, `scripts/check_matched_stats.py`, `scripts/check_thresholds.py` (QA용 유틸리티)
- `data/raw/news_backfill.csv` (15,877건), `data/raw/price_history.csv` (1년치)
- `data/processed/news_price_matched.csv` (8,140건), `data/processed/news_labeled.csv` (최종 라벨+split)

---

## 2026-08-23

### Phase 0 — 환경 세팅 완료
- Conda 환경 `unsloth_env` (Python 3.12.13) 생성
- PyTorch 2.11.0+cu130 설치, RTX 4060 Ti GPU 인식 확인
- unsloth 2026.8.19 + transformers/peft/trl/bitsandbytes/accelerate/datasets 설치
- Qwen3-4B 4bit 로드 테스트 성공 (`scripts/test_qwen_load.py`)
- **이슈**: `pip install unsloth`가 index-url 없이 실행되면서 GPU용 torch를 CPU 빌드로 덮어씀 → cu130 인덱스로 강제 재설치해서 해결. 이후 패키지 추가 설치 시 GPU 인식 재확인 필요.

### Phase 1 — 데이터 수집 맛보기 테스트
- yfinance로 주가 수집 파이프라인 검증 (문제없음)
- 뉴스 수집 소스 탐색:
  - 네이버 금융 종목뉴스 위젯(`finance.naver.com/item/news_news.naver`) — 개편으로 더 이상 작동 안 함
  - Google News RSS — 작동은 하나 노이즈(무관 언론사, 리다이렉트 링크) 있음
  - **네이버 뉴스검색 API** — developers.naver.com에서는 신규 등록 불가 (`"신규로 등록할 수 없는 API가 선택되었습니다"` 에러). 확인 결과 검색 API가 **네이버클라우드플랫폼(NCP)의 "NAVER API HUB"로 이전**됨. `console.ncloud.com`에서 신청, 엔드포인트 `https://naverapihub.apigw.ntruss.com/search/v1/news`, 헤더 `X-NCP-APIGW-API-KEY-ID` / `X-NCP-APIGW-API-KEY`로 인증 방식 변경됨
- 최종적으로 네이버 뉴스검색 API 연동 성공, `.env`에 키 저장 (`NAVER_API_KEY_ID`, `NAVER_API_KEY`)
- 20종목(7개 섹터 분산: 반도체/2차전지/자동차/플랫폼/바이오/금융/화학에너지/철강조선/통신/유통소비재)으로 뉴스 400건 + 주가 23일치 수집 테스트 완료
- **알려진 이슈**: 종목명 단순 키워드 검색이라 무관 기사(연예 뉴스 등)가 섞임 → Phase 2 라벨링 전 필터링 필요

### 다음 할 일
- [ ] Phase 1 본격화: 수집 기간 6개월~1년으로 확장, 종목당 뉴스량 확대
- [ ] 뉴스 노이즈 필터링 (경제/증권 섹션 위주, 키워드 동시 등장 조건)
- [ ] 뉴스-주가 매칭 로직 설계 (발행일 → 다음 거래일 종가 변동률)
- [ ] Phase 2: 라벨링 규칙(상승/하락/중립 임계값) 확정, EDA

### 산출물
- `scripts/collect_sample.py`, `scripts/collect_multi_sample.py`, `scripts/test_naver_news_api.py`, `scripts/test_qwen_load.py`
- `.env` / `.env.example` (API 키 관리)
- `data/raw/*.csv` (테스트 수집 결과, git 미포함)
