# RAG 데모 — 논조 분류 + 유사사례 검색 + Qwen 설명

2026-08-29~30 세션에서 구현. `models/klue-bert-qwen-sentiment-v4`(논조 분류기)를 실전에서 써먹는 형태로 확장한 것.

## 왜 이 구조인가

klue-bert-v4는 "긍정/부정/중립"만 반환하고 끝이라, 실사용에는 정보가 부족하다:
- 라벨 하나만으로는 "왜 그렇게 판단됐는지" 알 수 없음
- 단독으로는 "그래서 이게 의미가 있는지" 근거를 못 보여줌 (프로젝트 전체가 이미 논조-주가 상관관계는 약하다(r=0.04~0.13)는 결론을 낸 상태라, 이 근거를 계속 정직하게 노출하는 게 중요했음)

그래서 klue-bert 단독이 아니라, **검색으로 과거 유사사례 + 실제 결과를 붙이고, 그 근거를 Qwen이 설명하는 3단 구조**로 설계함.

## 아키텍처

```
사용자 입력(텍스트) 
    │
    ├─▶ ① klue-bert-qwen-sentiment-v4   → 논조 + 확신도          (즉시, <0.1초)
    │
    ├─▶ ② 임베딩(ko-sroberta) → 코사인유사도 검색(24,695건 중 top-5)
    │        → 각 사례의 실제 t+1/t+3/t+5/t+10 초과수익률 동봉    (~1초)
    │
    └─▶ ③ [버튼 클릭시만] Qwen3-4B가 ①②를 근거로 설명 생성      (첫 호출 ~32초, 이후 ~12초)
```

①②는 기본 흐름에서 항상 실행(가볍고 빠름), ③은 "추가 분석" 버튼을 눌러야만 실행됨 — Qwen(4bit, ~4~5GB VRAM)을 상시 로드해두지 않고 최초 클릭 시점에 지연 로딩하는 설계. RTX 4060 Ti(8GB) 기준 세 모델(klue-bert + 임베딩모델 + Qwen) 동시 로드 시 실측 VRAM 5.4GB로 여유 있음.

## 컴포넌트

| 파일 | 역할 |
|---|---|
| `scripts/build_news_embeddings.py` | 뉴스 24,695건을 `jhgan/ko-sroberta-multitask`로 임베딩 (1회성, 50초) |
| `scripts/rag_pipeline.py` | `SentimentRAG` 클래스 — 분류, 검색, Qwen 설명 생성을 하나로 묶음 |
| `api/main.py` | FastAPI. `POST /analyze`(①②), `POST /explain`(③, 지연로딩) |
| `web/` | Next.js 16 최소 UI — 텍스트 입력, 결과 표, "추가 분석" 버튼 |

## 정직성을 지키기 위한 설계 선택

- Qwen 프롬프트에 "상관관계가 약하고 표본이 적을 수 있으니 확정적 예측처럼 말하지 말 것"을 명시적으로 박아넣음 — LLM이 매끄러운 문장으로 약한 신호를 실제보다 그럴듯하게 포장할 위험을 프롬프트 레벨에서 차단
- 화면에도 "예측이 아니라 과거 통계 참고용"이라는 문구를 상시 노출
- 이 프로젝트가 하루 종일 검증한 결론(논조-수익률 상관관계 r=0.04~0.13, "작은 효과" 수준)을 도구 자체의 UX에 그대로 반영한 것

## 알려진 한계 (의도적으로 미룬 것)

- **텍스트 입력만 지원, 링크(URL) 스크래핑 미구현** — 스크래퍼는 사이트마다 구조가 달라 별도 작업 범위로 분리함
- **일반 커뮤니티 텍스트(반어법·은어)에는 성능 저하 확인됨** — klue-bert-v4가 뉴스/공시 스타일에서만 검증됨 (스팟체크: 뉴스·공시는 확신도 0.99~1.00, 커뮤니티체는 "중립" 회피 응답 + 낮은 확신도 0.71~0.85)
- **로컬 개발 서버만 존재, 배포 안 됨** (`localhost:3000` ↔ `localhost:8000`)
- **RAG 품질 검증이 수작업 스팟체크 수준** — 체계적인 검색 정확도 평가는 안 함

## 산출물 위치

- 임베딩: `data/processed/news_embeddings.npy`, `news_embeddings_meta.csv`
- 코드: `scripts/build_news_embeddings.py`, `scripts/rag_pipeline.py`, `api/main.py`, `web/`
